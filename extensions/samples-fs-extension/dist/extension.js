(()=>{"use strict";var e={398:e=>{e.exports=require("vscode")},729:e=>{e.exports=JSON.parse('{"Course 1":{"00 - Course 1 Welcome.webnb":["internal","# Welcome!\\n\\nWelcome to the course!\\n\\n- **Course 1** is an introduction to Web programming, including:\\n    - HTML fundamentals (module 1)\\n    - CSS fundamentals (module 2)\\n    - Programming fundamentals in JavaScript (Module 3)\\n    - JavaScript basics (module 4)\\n- **Course 2** goes deeper into JavaScript\\n- **Course 3** goes further\\n- **Course 4** focuses on frameworks\\n\\nThere are a combination of (1) readings like this, (2) videos, and (3) exercises. Beyond the readings and videos, **the exercises are crucial to your learning**.\\n\\n## What Will You Learn?\\n\\nThe focus of this specialization is on building *interactive* web applications. By \\"interactive\\", we mean applications that are responsive to user input (e.g., mouse clicks, keyboard input, etc.). We will learn *just enough* HTML and CSS to get started but the focus will be on **JavaScript**.\\n\\n### Why JavaScript?\\n\\nJavaScript is the language of the web and [the most widely used programming language in the world](https://survey.stackoverflow.co/2024/technology#most-popular-technologies-language). It powers the interactive elements of virtually every modern website. JavaScript can power web applications, mobile applications, desktop applications, and even server-side applications. It is a versatile language that can be used for a wide range of applications.\\n\\n## How to Use These Notebooks\\n\\n- If you want to load these on your own computer, you can (optionally):\\n    1. Install [Visual Studio Code](https://code.visualstudio.com/)\\n    2. Install [the WebNB extension](https://marketplace.visualstudio.com/items/?itemName=oney.web-notebook)\\n    3. Download [the course repository from GitHub](https://github.com/academic-innovation-online-learning/Practical-JavaScript)\\n- However, you can also just use the web version of the notebooks. You can run them in your browser without installing anything.\\n\\nThese notebooks come equipped with interactive exercises. For example, in the following code cell, try to:\\n\\n1. Change the number `1` to `5` so that it reads:\\n    ```javascript\\n    const x = 5;\\n    ```\\n2. Click the \\"Run\\" button to run the code cell.\\n3. Look at the output below the code cell, which should confirm that you completed the exercise correctly.\\n\\n```{javascript}\\nconst x = 1;\\n```"],"Module 1 - HTML":{"01 - Introduction to HTML.webnb":["internal","# Introduction to HTML\\n\\nBefore we dive into JavaScript, we need to learn some foundational HTML.\\n\\n## What is HTML?\\n\\n**HTML** (HyperText Markup Language) is the standard language for creating web pages. It is a \\"markup language\\"---a language for adding **structure** and **formatting** to text. More specifically, it is a markup language for creating \\"hypertext\\" documents, which are documents that contain links to other documents (the foundation of the World Wide Web).\\n\\n## Why HTML?\\n\\nAlthough our overall focus is on JavaScript, we first need to learn some foundational HTML. We will cover *just enough* HTML (and CSS) to get us started with JavaScript.\\n\\n## The Three Web Languages\\n\\nAlthough our focus is on JavaScript, Web development involves three primary languages:\\n\\n- **HTML** for defining the structure and content\\n- **CSS** for specifying how browsers should render/display this content\\n- **JavaScript** for defining the **behavior** of the page---how it reacts to things like clicks and keypresses\\n\\n\\nOne way to think about these three languages is to use an analogy with the human body:\\n![](js-body-analogy.png)\\n- **HTML** is the **skeleton** of the body, providing the structure and shape\\n- **CSS** is the **skin** of the body, providing the appearance and style\\n- **JavaScript** is the **muscles** of the body, providing the movement and interactivity\\n\\nJavaScript will be the focus of this specialization but we\'re going to cover a bit of HTML and CSS to get us enough of a foundation to started.\\n\\n\\n## Using these Notebooks\\n\\nThese notebooks are designed to be interactive. You can run the code cells, edit them, and see the results. You can also add new cells to write your own code.\\n\\nFor example, try editing the text in the following cell:\\n\\n```{html}\\n<p>Hello! Try \\"running\\" this by clicking me and then clicking the \\"play\\" button to my left.</p>\\n```"],"02 - Creating HTML Elements.webnb":["internal","# Creating HTML Elements\\n\\n**Elements** are the building blocks of HTML. Each element is part of the content or structure, like a heading, paragraph, or image.\\n\\n## Element Syntax\\n\\nThe \\"standard\\" way to create HTML elements is with two **tags**: an **opening tag** and a **closing tag**:\\n\\n- The **opening tag** is the name of the elements in angle brackets (`<tag>`) where `tag` is the type of element being created.\\n    - For example, the opening tag of a `p` (paragraph) element is `<p>`. The opening tag\\n- The **closing tag** is the same as the opening tag but with a forward slash (`/`) before the tag name (`</tag>`).\\n    - For example, the closing tag of a `p` (paragraph) element is `</p>`.\\n- The **content** of the element is placed between the opening and closing tags.\\n    - For example, to create a `p` element with the text content \\"Hello, World!\\", you would write:\\n        ```html\\n        <p>Hello, World!</p>\\n        ```\\n    - The content can be any text, HTML, or other elements.\\n\\nHere are a few examples of HTML elements:\\n\\n[A `p` element](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/p) represents a paragraph of text:\\n```html\\n<p>This is a paragraph.</p>\\n```\\n\\n[An `h1` element](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/h1) represents a level-1 heading (usually the main heading of a page):\\n```html\\n<h1>This is a level-1 heading.</h1>\\n```\\n\\nSo an element consists of three parts: the opening tag, the content, and the closing tag.\\n![](element_illustration.png)\\n\\nTry creating a `strong` element with the text `\\"hello\\"` in the code cell below:\\n\\n```{html}\\n```\\n\\n### Capitalization\\nElements can have any case (capitalization) but the convention is to use lowercase. For example, `<P>` and `<p>` are equivalent but `<p>` is preferred. The same applies to the closing tag: `</P>` and `</p>` are equivalent but `</p>` is preferred.\\n\\n\\n## Self-Closing Tags\\n\\nSome elements do not have any content and are \\"self-closing\\". These elements do not require a closing tag. For example, the `<br>` element represents a line break and does not have any content. It is a self-closing tag:\\n\\n```html\\n<br />\\n```\\n\\nIn modern HTML, the self-closing tag is optional and you can just write `<br>`. However, it is a good practice to include the closing slash (`/`) for self-closing tags to make it clear that they are self-closing.\\n\\nIn the code cell below, create an `<hr>` (horizontal rule) element. This element is a self-closing tag that creates a horizontal line on the page. You can use it to separate content or create a visual break in the page.\\n\\n```{html}\\n```\\n\\n## Exercises\\n\\n1. Create a paragraph element (`p`) with the text \\"Hello, World!\\" and run the cell to see the result.\\n\\n    ```{html}\\n    ```\\n\\n2. Create a level-1 heading element (`h1`) with the text \\"My First Heading\\" and run the cell to see the result.\\n\\n    ```{html}\\n    ```\\n\\n3. Create a level-2 heading element (`h2`) with the text \\"My Second Heading\\" and run the cell to see the result.\\n\\n    ```{html}\\n    ```"],"03 - Foundational HTML Elements.webnb":["internal","# Foundational HTML Elements\\n\\nThere are *many* different types of HTML elements---far too many to fully cover in this (or any other course). [This page contains a list of all the different types of HTML elements](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements) for reference. We will cover the most essential ones here.\\n\\n## Document Structure\\n\\nPretty much every HTML document will have the same top-level structure. This is the basic structure of an HTML document:\\n\\n```html\\n<!DOCTYPE html>\\n<html>\\n    <head>\\n    </head>\\n    <body>\\n    </body>\\n</html>\\n```\\n\\n- `<!DOCTYPE html>`: This tells the browser which version of HTML to use (the latest version, HTML 5).\\n- `<html>`: This is the \\"root\\" element of every document, meaning all other elements are contained within this element.\\n- `<head>`: This element contains metadata about the document, such as the title. It is not displayed in the browser.\\n- `<body>`: This element contains the content of the document, such as text, images, and links. It is displayed in the browser.\\n\\n## Content Elements\\nThese elements are used to display text in different ways. They can be used to create headings, paragraphs, lists, and more.\\n\\n- `<h1>` to `<h6>`: These are heading elements, with `<h1>` being \\"level 1\\" (the largest) and `<h6>` being \\"level 6\\" (the smallest). They are used to create headings and subheadings.\\n- `<p>`: This is a paragraph element. It is used to create paragraphs of text.\\n- `<strong>`: This element is used to create strong emphasis on text. It is typically displayed in **bold**. \\n- `<em>`: This element is used to create emphasis on text. It is typically displayed in *italics*.\\n- `<ul>`: This is an unordered list element. It is used to create a list of items that do not have a specific order. Each item in the list is contained within a `<li>` (list item) element.\\n- `<ol>`: This is an ordered list element. It is used to create a list of items that have a specific order. Each item in the list is contained within a `<li>` (list item) element.\\n- `<li>`: This is a list item element. It is used to create items in a list, either ordered (`ol`) or unordered (`ul`).\\n\\n## Interactive Elements\\n\\nThese elements are used to create interactive elements, such as links and buttons.\\n- `<a>`: This is an anchor element. It is used to create links to other pages or sections of the same page.\\n- `<button>`: This element is used to create buttons that can be clicked to perform an action. It can contain text or other elements, such as images or icons.\\n- `<input>`: This element is used to create input fields, such as text boxes, checkboxes, and radio buttons. It can be used to collect user input.\\n\\n## Generic Containers\\n\\nThese elements have no *semantic* meaning (i.e., they don\'t convey any specific meaning about the content they contain). They are useful for grouping elements together, applying styles, or manipulating them with JavaScript.\\n\\n- `<div>`: A generic block-level container (think of \\"block-level\\" as meaning that it starts on a new line and takes up the full width). It has no semantic meaning, but is useful for grouping elements together.\\n- `<span>`: A generic *inline* container (think of \\"inline\\" as meaning that it only takes up as much width as its content needs). Like a `div`, it has no semantic meaning, but is useful for grouping elements together."],"04 - Nesting HTML Elements.webnb":["internal","# Nesting HTML Elements\\n\\nThe content of an element can contain other elements. This is called *nesting*. For example, in the following code:\\n\\n```html\\n<p>This is a <strong>bolded</strong> word.</p>\\n```\\n\\nThe `<strong>` element is nested inside the `<p>` element. This means that the text \\"bolded\\" will be displayed in bold, while the rest of the text will be displayed normally.\\n\\n## Terminology\\n\\nIn the code sample above (`<p>This is a <strong>bolded</strong> word.</p>`), there is standard terminology:\\n\\n- The `<p>` element is the *parent* of the `<strong>` element.\\n- The `<strong>` element is the *child* of the `<p>` element.\\n\\nWhenever nesting elements, it is important to make sure elements are *fully nested*, meaning that every child element is fully contained within its parent element. For example, the following code is not fully nested:\\n\\n```html\\n<p>This would <em>not be</p> fully nested.</em>\\n```\\nIn the example above, the `<em>` element (which creates emphasized text that is italicized) is not fully contained within the `<p>` element, which makes it invalid HTML (though browsers will try to fix it). We could fix it by fully moving the `<em>` element inside its parent, such as:\\n\\n```html\\n<p>This would <em>be</em> fully nested.</p>\\n```\\n\\n## Exercises\\n\\n1. Create a `p` element that contains a `strong` element. The text content of both elements can be *anything*, as long as the `strong` element is fully contained within the `p` element.\\n\\n    ```{html}\\n    ```"],"05 - HTML Element Trees.webnb":["internal","# HTML Element Trees\\n\\nHTML elements can contain other elements (nesting). This nesting can go as deep as you want; one element can contain another, which can contain another, and so on. This creates a tree-like structure of elements. For example, the following code creates a tree of elements:\\n\\n```html\\n<html>\\n    <head>\\n        <title>My Page</title>\\n    </head>\\n    <body>\\n        <h1>My Page</h1>\\n        <p>This is a paragraph.</p>\\n        <ul>\\n            <li>Item 1</li>\\n            <li>Item 2</li>\\n            <li>Item 3</li>\\n        </ul>\\n    </body>\\n</html>\\n```\\n\\nThis code creates a tree of elements, which can be represented as follows:\\n\\n- `html`\\n    - `head`\\n        - `title`\\n            - *#text: \\"My Page\\"*\\n    - `body`\\n        - `h1`\\n            - *#text: \\"My Page\\"*\\n        - `p`\\n            - *#text: \\"This is a paragraph.\\"*\\n        - `ul`\\n            - `li`\\n                - *#text: \\"Item 1\\"*\\n            - `li`\\n                - *#text: \\"Item 2\\"*\\n            - `li`\\n                - *#text: \\"Item 3\\"*\\n\\n## Terminology\\n\\n- `html` is the **root** element of the tree.\\n    - The root element is the top-level element that contains all other elements.\\n- `h1` is a **child** of `body`.\\n    - A child element is an element that is contained within another element.\\n- `body` is the **parent** of `h1`.\\n    - A parent element is an element that contains other elements.\\n- `h1` and `p` are **siblings**—they share the same parent (`body`).\\n    - Sibling elements are elements that share the same parent.\\n- `html` is an **ancestor** of `p`.\\n    - An ancestor element is any element that is higher up in the tree than the current element.\\n- `p` is a **descendant** of `html`.\\n    - A descendant element is any element that is lower down in the tree than the current element.\\n\\n## Exercises\\n\\n### Write HTML to Match the Tree\\n\\n1. Match this tree:\\n\\n    - `html`\\n        - `body`\\n            - `h1`\\n                - *#text: \\"Welcome\\"*\\n            - `p`\\n                - *#text: \\"This is my website.\\"*\\n\\n   Write HTML code that matches this structure.\\n   ```{html}\\n   ```\\n\\n2. Match this tree:\\n\\n    - `html`\\n        - `body`\\n            - `ul`\\n                - `li`\\n                    - *#text: \\"Apples\\"*\\n                - `li`\\n                    - *#text: \\"Oranges\\"*\\n                - `li`\\n                    - *#text: \\"Bananas\\"*\\n\\n   Write HTML code to create an unordered list that matches this tree.\\n\\n   ```{html}\\n   ```\\n\\n3. Match this tree:\\n\\n    - `html`\\n        - `body`\\n            - `p`\\n                - *#text: \\"This is \\"*\\n                - `strong`\\n                    - *#text: \\"important\\"*\\n                - *#text: \\" information.\\"*\\n\\n   Write HTML that produces a paragraph with the word \\"important\\" in bold.\\n   ```{html}\\n   ```\\n"],"06 - Element Attributes.webnb":["internal","# Element Attributes\\n\\n**Attributes** provide additional information about an element. For example, an attribute for an `<a>` (link) element might specify *where* the link goes. An attributes for an `<img>` (image) element might specify *which image* to display. Attributes have a *name* and a *value*, specified in the opening tag of the element. For example, the following code creates a link to Google:\\n```html\\n<a href=\\"https://www.google.com\\">Google</a>\\n```\\n\\nIn this code, `href` is the name of the attribute and `https://www.google.com` is the value of the attribute. The value is enclosed in double quotes (`\\"`). The `href` attribute specifies the URL that the link goes to when clicked.\\n\\nElements can also have multiple attributes. For example, the following code creates an image with a `src` (source) attribute that specifies the URL of the image and an `alt` (alternative text) attribute that specifies the text to display if the image cannot be loaded:\\n```html\\n<img src=\\"https://www.example.com/image.jpg\\" alt=\\"An example image\\">\\n```\\n\\nHere, the `<img>` element has two attributes: `src` and `alt`. The `src` attribute specifies the URL of the image and the `alt` attribute specifies the alternative text to display if the image cannot be loaded.\\n\\n## Boolean Attributes (Attributes Without Values)\\n\\nIn some cases, the meaning of an attribute is determined by its presence or absence. For example, the `disabled` attribute for `<button>` elements specifies that the button is disabled and cannot be clicked. We could set the value to be anything; `disabled=\\"true\\"`, `disabled=\\"disabled\\"`, `disabled=\\"false\\"`, or even `disabled=\\"\\"` (an empty string) would all make the `<button />` disabled.\\n\\nIn these cases, we can omit the value and just write the attribute name. For example, the following code creates a disabled button:\\n```html\\n<button disabled>You cannot click me</button>\\n```\\n\\n## Exercises\\n\\n1. Create a link (`<a>`) element that links to `https://www.umich.edu/` and has the text \\"University of Michigan\\". The link should open in a new tab when clicked.\\n    ```{html}\\n    ```\\n\\n2. Create an `<input>` element with the following attributes:\\n    - `type` set to `text`\\n    - `placeholder` set to \\"Enter your name\\"\\n\\n    ```{html}\\n    ```\\n3. Create an `<img>` element with the following attributes:\\n    - `src` set to `https://www.example.com/image.jpg`\\n    - `alt` set to \\"An example image\\"\\n    - `width` set to `200`\\n    - `height` set to `100`\\n\\n    ```{html}\\n    ```"],"07 -  Common Attributes.webnb":["internal","# Common Attributes\\n\\nSome attributes are specific to certain elements. Some of the most important attributes are:\\n\\n- `<a>` (anchor) elements can set:\\n    - `href`: Specifies the URL of the page the link goes to.\\n- `<img>` (image) elements can set:\\n    - `src`: Specifies the URL of the image.\\n    - `alt`: Specifies the alternative text to display if the image cannot be loaded\\n- `<button>` and `<input>` elements can set:\\n    - `disabled`: Specifies that the element is disabled and cannot be clicked.\\n\\nThere are also some attributes that are common to many elements. These include:\\n- `id`: Specifies a unique identifier for the element. This will be used to reference the element from JavaScript or CSS.\\n- `class`: Specifies one or more class names for the element. Like `id`, this will be used to reference the element from JavaScript or CSS. Unlike `id`, the class name does not have to be unique. You can use the same class name for multiple elements."],"08 - HTML Comments.webnb":["internal","# HTML Comments\\n\\n**Comments** are parts of the code that are ignored by browsers. They are normally used to either (1) add notes to the code or (2) temporarily remove parts of the code without deleting them..\\n\\n## Syntax\\n\\nHTML comments start with `\x3c!--` and end with `--\x3e`. For example, the following code contains a comment:\\n\\n```html\\n\x3c!-- This is a comment --\x3e\\n<p>This is a paragraph.</p>\\n```\\n\\nThe comment will not be displayed in the browser, but it can be seen in the HTML source code. Comments can be placed anywhere in the code, even inside other elements. For example:\\n\\n```html\\n<p>This is a paragraph. \x3c!-- This is a comment --\x3e</p>\\n```\\n\\nMost code editors have keyboard shortcuts to add comments. In Visual Studio Code, for example, you can use `Ctrl + /` (Windows) or `Cmd + /` (Mac) to add or remove comments."],"09 - Semantic HTML.webnb":["internal","# Semantic HTML\\n\\nThere are a *lot* of HTML elements. You are not expected to memorize them all. The reason there are so many elements is that each one is designed to convey a specific **meaning**.\\n\\nFor example:\\n\\n* `<h1>` represents a top-level heading.\\n* `<a>` creates a link.\\n* `<img>` displays an image.\\n* ...etc.\\n\\nThese elements are not just for presentation—they communicate what kind of content they contain. When you choose an element that matches the meaning or purpose of your content, you are writing **semantic HTML**.\\n\\n## Why Semantics Matter\\n\\nSemantic HTML helps both **people** and **machines** understand your web page. When a page uses the correct elements:\\n\\n* Screen readers can announce the content more clearly for users with visual impairments.\\n* Search engines can better index and rank your content.\\n* Developers (including future you) can more easily understand the structure and purpose of the page.\\n\\nFor instance, the main heading of a page should use an `<h1>` tag:\\n\\n```html\\n<h1>My Page</h1>\\n```\\n\\nThis is not just a style choice—it tells browsers, assistive technologies, and search engines that this is the main title of the page.\\n\\n## What Not to Do: Overusing Generic Containers\\n\\nHTML also includes generic container elements: `<div>` and `<span>`. These are useful for grouping content, but they do not carry any semantic meaning.\\n\\n* `<div>` is a block-level element. It starts on a new line and stretches the full width available.\\n* `<span>` is an inline element. It only takes up as much space as its content needs.\\n\\nHere is what using them might look like:\\n\\n```html\\n<div>This is an example of a div</div>\\n<span>My Page</span>\\n```\\n\\nWhile this may *look* fine in a browser, these elements tell us nothing about the meaning of the content. A screen reader wouldn not know that \\"My Page\\" is supposed to be a heading.\\n\\n## Semantic HTML in this Specialization\\n\\nIn this specialization, we will *not* always follow best practices for semantic HTML. That is because our main focus is learning JavaScript. So you will sometimes see `<div>` and `<span>` used as shortcuts.\\n\\nStill, whenever possible, you should use the most appropriate, meaningful element for the job. Writing semantic HTML makes your code cleaner, more accessible, and easier to maintain."],"element_illustration.png":["external",16028],"js-body-analogy.png":["external",127399]},"Module 2 - CSS":{"01 - Introduction to CSS.webnb":["internal","# Introduction to CSS\\n\\nNow that we\'ve covered the essentials of HTML—the language we use to describe the structure of a webpage, it is time to turn to CSS.\\n\\n**CSS** stands for **Cascading Style Sheets**. While HTML defines the **structure** of a webpage, CSS describes how that structure should **look**. In other words, if HTML is the skeleton of a webpage, CSS is the skin—it controls things like colors, fonts, spacing, borders, and layout.\\n\\nWith CSS, you can:\\n\\n- Change the color of text\\n- Set background images or colors\\n- Adjust spacing and alignment\\n- Control the layout of elements on the page\\n- Apply visual effects like shadows, transitions, and animations\\n\\n## Three Ways to Add CSS\\n\\nThere are three ways to apply CSS to a webpage:\\n\\n### **Inline CSS**\\n\\nYou can apply styles directly to an individual HTML element using the `style` attribute:\\n\\n```html\\n<p style=\\"color: blue;\\">This is blue text.</p>\\n```\\n\\nThis is the easiest way to get started, and it\'s helpful for quick tests or demos. However, it should usually be avoided in real projects, since it mixes content with styling and makes code harder to maintain.\\n\\n### **Internal CSS**\\n\\nYou can write CSS inside a `<style>` element in the `<head>` of your document:\\n\\n```html\\n<head>\\n  <style>\\n    p {\\n      color: green;\\n    }\\n  </style>\\n</head>\\n```\\n\\nThis keeps your styles grouped together, separate from the HTML content.\\n\\n### **External CSS**\\n\\nThe best practice is to put your CSS rules in a separate `.css` file and link to it from your HTML:\\n\\n```html\\n<head>\\n  <link rel=\\"stylesheet\\" href=\\"styles.css\\" />\\n</head>\\n```\\n\\nThis allows you to reuse styles across multiple pages and keeps your HTML cleaner."],"02 - CSS Syntax.webnb":["internal","# CSS Syntax\\n\\nIn CSS, we declare **rules** that tell the browser how to style HTML elements. A rule consists of a **selector** and a **declaration block**:\\n\\n- The **selector** specifies which HTML element(s) the rule applies to\\n  - The selector can be an HTML tag (e.g., `p`, `h1`, etc.), a class (e.g., `.class-name`), an ID (e.g., `#id-name`), or a combination of these (which we\'ll cover later)\\n- The **declaration block** specifies what styles to apply to the selected element(s), via **declarations**, each of which consists of:\\n  - a **property** (e.g., `color`, `font-size`, etc.)\\n  - a **value** (e.g., `red`, `16px`, etc.)\\n    - The property and value are separated by a colon (`:`)\\n- Each declaration is separated by a semicolon (`;`)\\n\\n## Example\\n```css\\np {\\n  color: blue;\\n  font-size: 16px;\\n}\\n```\\nIn this example:\\n- The selector is `p`, which means this rule applies to all `<p>` elements\\n- The declaration block contains two declarations:\\n  - `color: blue;` sets the text color to blue\\n  - `font-size: 16px;` sets the font size to 16 pixels\\n- Each declaration ends with a semicolon\\n\\nWe can also add comments in CSS using `/* comment */` syntax. Comments are ignored by the browser and are useful for explaining your code or leaving notes for yourself or others.\\n\\n```css\\n/* This is a comment */\\np {\\n  color: blue; /* This sets the text color to blue */\\n  font-size: 16px; /* This sets the font size to 16 pixels */\\n}\\n```\\n\\nThere are three ways to add CSS to documents, each covered in the next readings:\\n\\n1. **Inline**: adds CSS declarations directly in the HTML element\\n2. **Internal**: declares CSS rules in a `<style>` element (typically in the `<head>` section of the HTML document)\\n3. **External**: in a separate `.css` file\\n\\nWe\'ll start with inline CSS because it\'s the simplest, even though generally speaking, you should avoid it. Then we\'ll move on to internal and external CSS files."],"03 - Inline CSS.webnb":["internal","# Inline CSS\\n\\nSo let\'s say we have a simple HTML document like this:\\n\\n```html\\n<!DOCTYPE html>\\n<html>\\n  <body>\\n    <p>Hello, world!</p>\\n  </body>\\n</html>\\n```\\n\\n...and we want to make the text red. We can do this by adding a `style` attribute to the `p` element:\\n\\n```html\\n<!DOCTYPE html>\\n<html>\\n  <body>\\n    <p style=\\"color: red\\">Hello, world!</p>\\n  </body>\\n</html>\\n```\\n\\nThe `style` attribute is a way to add CSS directly to an HTML element. It\'s called \\"inline\\" because the CSS is \\"in line\\" with the HTML.\\n\\nThe value of the `style` attribute is essentially a different language (CSS). So essentially *this* string (highlight) is a different language (CSS). And again, CSS is a language for describing how the browser should display elements.  So let\'s look at the syntax of CSS.\\n\\nWe can see here that we set the `color` property to `red`. The `color` property is a CSS property that describes the color of the text. [There are many CSS properties.](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference). We\'ll go over some of the essentials but `color` is one to remember.\\n\\nSo in the syntax of CSS, we have a property and a value, separated by a colon. Just like with attributes, there property names are built in so we can\'t just make up our own. [Just look up property names](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference). In this case, `color` is a valid property name that controls the text color.\\n\\nAnd then after the property name, we have a colon. And then we have the value. The value of the `color` property can be set to any [color name](https://developer.mozilla.org/en-US/docs/Web/CSS/named-color), [hex code](https://developer.mozilla.org/en-US/docs/Web/CSS/hex-color), or there are other ways to set the color. So in this case, we\'re setting the color to red.\\n\\nIf we want to add multiple CSS declarations, we can separate them with semicolons. So let\'s say we want to make the text red and bold. We can do that like this:\\n\\n```html\\n<!DOCTYPE html>\\n<html>\\n  <body>\\n    <p style=\\"color: red; font-weight: bold\\">Hello, world!</p>\\n  </body>\\n</html>\\n```\\n\\nSo we have the `color` property set to `red` and then we have a semicolon to separate multiple declarations. And then we have the `font-weight` property set to `bold`. If we wanted to set the background color to yellow, we could do that like this:\\n\\n```html\\n<!DOCTYPE html>\\n<html>\\n  <body>\\n    <p style=\\"color: red; font-weight: bold; background-color: yellow\\">Hello, world!</p>\\n  </body>\\n</html>\\n```\\n\\nSo this all looks perfectly fine but I mentioned that you should avoid inline CSS. So why is that?\\n\\nWell, it\'s not very maintainable. This is a simple example with just one `<p>` element but imagine if we had a lot of elements with inline CSS. If we wanted to change the color of all of them, we\'d have to change each one individually. And if we wanted to change the color of just one of them, we\'d have to find it in the HTML and change it there. So it\'s not very maintainable.\\n\\n\\nSo this is **not** how we\'ll typically use CSS in this course. In the next readings, we\'ll talk how to use CSS in a more maintainable way."],"04 - The style Element.webnb":["internal","# The `<style>` Element\\n\\nLast time, we talked about inline CSS, which is when we add CSS directly to an HTML element. However, this is not very maintainable. So let\'s talk about another way to add CSS to a webpage: internal CSS. Internal CSS is when we add CSS to a `<style>` element in the `<head>` of the document.\\n\\n\\n```html\\n<!DOCTYPE html>\\n<html>\\n  <head>\\n    <style>\\n      p {\\n        color: red;\\n        font-weight: bold;\\n        background-color: yellow;\\n      }\\n    </style>\\n  </head>\\n  <body>\\n    <p>Hello, world!</p>\\n  </body>\\n</html>\\n```\\n\\nHere, we have a `<style>` element in the `<head>` of the document. The `<style>` element is a way to add CSS to a webpage. And we can see that we have a CSS declaration inside the `<style>` element. This declaration is identical to the inline CSS declaration from the last video. We have the `color` property set to `red`, the `font-weight` property set to `bold`, and the `background-color` property set to `yellow`. So this is a way to add CSS to a webpage.\\n\\n\\nBut you\'ll also notice that we have a `p` before the declaration. This is called a **selector**. A selector is a way to specify which elements the CSS should apply to. In this case, we\'re saying that this CSS should apply to all `<p>` elements. Then, we have curly braces to start and end the set of declarations that will apply to the `<p>` elements.\\n\\nSo if we added more `<p>` elements, they would all have red text, bold text, and a yellow background.\\n\\n```html\\n<!DOCTYPE html>\\n<html>\\n  <head>\\n    <style>\\n      p {\\n        color: red;\\n        font-weight: bold;\\n        background-color: yellow;\\n      }\\n    </style>\\n  </head>\\n  <body>\\n    <p>Hello, world!</p>\\n    <p>This is another paragraph!</p>\\n  </body>\\n</html>\\n```\\n\\nWe could also add a selector for `<h1>` elements, like this:\\n\\n```html\\n<!DOCTYPE html>\\n<html>\\n  <head>\\n    <style>\\n      p {\\n        color: red;\\n        font-weight: bold;\\n        background-color: yellow;\\n      }\\n      h1 {\\n        color: blue;\\n      }\\n    </style>\\n  </head>\\n  <body>\\n    <h1>Heading</h1>\\n    <p>Hello, world!</p>\\n    <p>This is another paragraph!</p>\\n  </body>\\n</html>\\n```\\n\\nSo now we have two selectors: one for `<p>` elements and one for `<h1>` elements. And we can see that the `<h1>` elements have blue text. So we can use selectors to specify which elements the CSS should apply to. But these selectors have their own syntax.\\n\\n## Exercises\\n\\n1. Draft a simple HTML document with a `<style>` element in the `<head>` section. Inside the `<style>` element, add CSS rules to change the text color of all `<h1>` elements to blue and the background color of all `<p>` elements to light gray.\\n\\n    ```{html}\\n    <html>\\n        <head>\\n            \x3c!-- Create a style element below this comment --\x3e\\n        </head>\\n        <body>\\n            <h1>Heading</h1>\\n            <p>This is a paragraph.</p>\\n        </body>\\n    </html>\\n    ```"],"05 - External CSS.webnb":["internal","# External CSS\\n\\nSo we\'ve seen how to use inline CSS (where we add CSS in a `style` attribute in an HTML element) and internal CSS (where we add CSS in a `<style>` element in the `<head>` of the document). But one problem with internal CSS is that it can be quite long and so sometimes, we want to put our CSS in a separate file. This is called **external CSS**.\\n\\nTo use external CSS, we need to create a separate file with a `.css` extension. For example, we could create a file called `styles.css` and put it in the same directory as our HTML file. Then, we can add a `<link>` element in the `<head>` of our HTML file to link to the CSS file.\\n\\n```html\\n<!DOCTYPE html>\\n<html>\\n  <head>\\n    <link rel=\\"stylesheet\\" href=\\"styles.css\\">\\n  </head>\\n  <body>\\n    <p>Hello, world!</p>\\n  </body>\\n</html>\\n```\\n\\nThe `href` attribute specifies the path to the CSS file. In this case, the CSS file is in the same directory as the HTML file so we just need to specify the name of the file. If the CSS file was in a different directory, we would need to specify the path to the file.\\n\\n`styles.css`:\\n\\n```css\\np { color: red; }\\n```\\n\\n\\nExternal CSS also can make it easier to share CSS between multiple HTML files. For example, let\'s say we have two HTML files: `index.html` and `about.html`. We want both of them to have the same CSS. Then, we can create a `styles.css` file and link to it in both HTML files.\\n\\nWe can also include CSS files that other people wrote. For example, let\'s say we want to use [Bootstrap](https://getbootstrap.com/). Bootstrap is a CSS framework that makes it easy to create responsive websites. We can see that on the Bootstrap website, they specify how to include Bootstrap in our HTML file...\\n\\nAs soon as we do, you can see some of the style starting to change. Andi f you look at the bootstrap documentation, you\'ll see that we can do things like have buttons with different colors:\\n\\n```html\\n<button type=\\"button\\" class=\\"btn btn-primary\\">Primary</button>\\n<button type=\\"button\\" class=\\"btn btn-secondary\\">Secondary</button>\\n<button type=\\"button\\" class=\\"btn btn-success\\">Success</button>\\n```\\n\\nSo, in sum, the `<link>` element is a way to include external CSS in our HTML file. And we can use it to include CSS files that we wrote or CSS files that other people wrote."],"06 - Essential Selectors.webnb":["internal","# Essential CSS Selectors\\n\\nCSS **selectors** are a way of selecting elements or in other words, specifying which elements the CSS should apply to. So...if selectors sound simple, just know that they can actually get fairly complicated...but if it sounds complicated, know that they can also be pretty simple. So let\'s start with the simple stuff.\\n\\nIn this video, I\'m going to cover what I consider to be the essential selectors.\\n\\n\\nFirst, we have the **tag selector**. This is a selector that selects all elements of a given tag. So if we have a `<p>` element, we can select it like this:\\n\\n```css\\np { color: red; } /* Applies to all paragraphs */\\n```\\n\\nSo this selector selects all `<p>` elements and then we have the curly braces to start and end the set of declarations that will apply to the `<p>` elements. And then we have the `color` property set to `red`. So this will make all `<p>` elements have red text.\\n\\nOK so simple enough...if we want to select every element with a given tag, we just put the tag name. `a` for `<a>` elements, `h1` for `<h1>` elements, `div` for `<div>` elements, etc.\\n\\n\\n\\n## Class Selectors\\n\\nNext, suppose we instead want to select all elements with a given `class`. We can do that with a **class selector**. So let\'s say we have a `<p>` element with `class=\\"highlighted\\"`:\\n\\n```html\\n<p class=\\"highlighted\\">Hello, world! This paragraph is highlighted.</p>\\n<p>This paragraph is not highlighted.</p>\\n<p>...and neither is this one.</p>\\n```\\n\\nNow, we don\'t want **every** `<p>` element to be highlighted. We only want the ones with `class=\\"highlighted\\"` to be highlighted. So we can do that with a class selector:\\n\\n```css\\n.highlighted { background-color: yellow; } /* Applies to everything with the \\"highlighted\\" class */\\n```\\n\\nHere, we added a `.` before the class name. This is how we specify that we want to select all elements with a given **class**. If we didn\'t have the `.`, it would be a tag selector and would look for all `<highlighted>` elements, which don\'t exist.\\n\\nOne thing to note here is that this class selector will select **all** elements with `class=\\"highlighted\\"`, not just `<p>` elements. So if we have a `<div>` element with `class=\\"highlighted\\"`, it will also be highlighted.\\n\\n```html\\n<p class=\\"highlighted\\">Hello, world! This paragraph is highlighted.</p>\\n<p>This paragraph is not highlighted.</p>\\n<p>...and neither is this one.</p>\\n<div class=\\"highlighted\\">Also highlighted.</div>\\n<div>Not highlighted.</div>\\n```\\n\\n# ID Selectors\\n\\nFinally, we have the **ID selector**. This is a selector that selects the element with a given `id`. So let\'s say we have a `<p>` element with `id=\\"footer\\"`.\\n\\n```html\\n<p id=\\"footer\\">This is the footer.</p>\\n```\\n\\n\\nWe could write a selector like this:\\n\\n```css\\n#footer { color: red; } /* Applies to the element with id \\"footer\\" */\\n```\\n\\nHere, we added a `#` before the ID name. This is how we specify that we want to select the element with a given **ID**. If we didn\'t have the `#`, it would be a tag selector and would look for all `<footer>` elements. If we had a `.`, it would be a class selector and would look for all elements with `class=\\"footer\\"`.\\n\\n\\nSo we\'ve learned how to select elements by tag, by class, and by ID. But we can also combine selectors to create more specific selectors. So let\'s cover that in the next video\\n\\n# Combining Selectors\\n\\nSo we can combine selectors to create more specific selectors. For example, let\'s say we want to select all `<p>` elements with `class=\\"highlighted\\"`:\\n\\n```css\\np.highlighted { background-color: yellow; } /* Applies to all <p> elements with class=\\"highlighted\\" */\\n```\\n\\nHere, we can combine multiple selectors by putting them together. The first part specifies that it must be a `<p>` element and the second part specifies that it must have `class=\\"highlighted\\"`.\\n\\nWe could also combine multiple class selectors:\\n\\n```css\\np.highlighted.selected { background-color: yellow; } /* Applies to all <p> elements with class=\\"highlighted\\" and class=\\"selected\\" */\\n```\\n\\nThis will select all `<p>` elements with `class=\\"highlighted\\"` and `class=\\"selected\\"`.\\n\\n# Descendant Selectors\\n\\nWe can also select elements that are descendants of other elements. For example, let\'s say we have a `<div>` element with `id=\\"main_panel\\"` and we want to select all `<p>` elements that are descendants of that `<div>` element:\\n\\n```html\\n<div id=\\"main_panel\\">\\n  <p>This paragraph is a descendant of the element with id=\\"main_panel\\"</p>\\n</div>\\n<p>This paragraph is not a descendant of the element with id=\\"main_panel\\"</p>\\n```\\n\\nWe can select all `<p>` elements that are descendants of the element with `id=\\"main_panel\\"` like this:\\n\\n```css\\n#main_panel p { color: red; } /* Applies to all <p> elements that are descendants of the element with id=\\"main_panel\\" */\\n```\\n\\nHere, we have the ID selector for the `<div>` element and then we have a space and then we have the tag selector for the `<p>` element. So this will select all `<p>` elements that are descendants of the element with `id=\\"main_panel\\"`.\\n\\nSo by adding a space between selectors, we can select elements that are descendants of other elements.\\n\\nThere are many more selectors but these are the ones that I consider to be the essential ones. So if you know these, you\'ll be able to do a lot with CSS."],"07 - Combining Selectors.webnb":["internal","# Combining CSS Selectors\\n\\nWe can combine selectors to create more specific selectors. For example, let\'s say we want to select all `<p>` elements with `class=\\"highlighted\\"`:\\n\\n```css\\np.highlighted { background-color: yellow; } /* Applies to all <p> elements with class=\\"highlighted\\" */\\n```\\n\\nHere, we can combine multiple selectors by putting them together. The first part specifies that it must be a `<p>` element and the second part specifies that it must have `class=\\"highlighted\\"`.\\n\\nWe could also combine multiple class selectors:\\n\\n```css\\np.highlighted.selected { background-color: yellow; } /* Applies to all <p> elements with class=\\"highlighted\\" and class=\\"selected\\" */\\n```\\n\\nThis will select all `<p>` elements with `class=\\"highlighted\\"` and `class=\\"selected\\"`.\\n\\n# Descendant Selectors\\n\\nWe can also select elements that are descendants of other elements. For example, let\'s say we have a `<div>` element with `id=\\"main_panel\\"` and we want to select all `<p>` elements that are descendants of that `<div>` element:\\n\\n```html\\n<div id=\\"main_panel\\">\\n  <p>This paragraph is a descendant of the element with id=\\"main_panel\\"</p>\\n</div>\\n<p>This paragraph is not a descendant of the element with id=\\"main_panel\\"</p>\\n```\\n\\nWe can select all `<p>` elements that are descendants of the element with `id=\\"main_panel\\"` like this:\\n\\n```css\\n#main_panel p { color: red; } /* Applies to all <p> elements that are descendants of the element with id=\\"main_panel\\" */\\n```\\n\\nHere, we have the ID selector for the `<div>` element and then we have a space and then we have the tag selector for the `<p>` element. So this will select all `<p>` elements that are descendants of the element with `id=\\"main_panel\\"`.\\n\\nSo by adding a space between selectors, we can select elements that are descendants of other elements.\\n\\nThere are many more selectors but these are the ones that I consider to be the essential ones. So if you know these, you\'ll be able to do a lot with CSS."],"08 - Essential CSS Properties.webnb":["internal","# Essential CSS Properties\\n\\n- `color`: text color\\n- `background-color`: background color\\n- `width` and `height`: (e.g., `width: 100px;`)\\n- `font-size`: font size (e.g., `font-size: 20px;`)\\n- `font-weight`: font weight (e.g., `font-weight: bold;`)\\n- `text-align`: text alignment (e.g., `text-align: center;`)\\n\\nThere are many more and some that it\'s a little tough for me to not go over (particularly `margin`, `padding`, `border`, and `display`). But if you know these, you\'ll know enough for this class."],"08 - Semantic HTML and Appearances.webnb":["internal","# Semantic HTML and Appearances\\n\\nRecall that HTML elements have [meaning](https://developer.mozilla.org/en-US/docs/Glossary/Semantics#semantics_in_html) and that using the **correct** element helps make your page **understandable** (for other people and for search engines).\\n\\nBut now, we can use CSS to style elements differently. So let me ask you a question: given the choice between using CSS and HTML to style an element, which should you use?\\n\\n```html\\n<!DOCTYPE html>\\n<html>\\n    <head>\\n        <style>\\n            .header {\\n                font-size: 2em;\\n                font-weight: bold;   \\n            }\\n        </style>\\n    </head>\\n    <body>\\n        <h1>This was styled using an h1</h1>\\n        <div class=\\"header\\">This was styled using CSS</div>\\n    </body>\\n</html>\\n```\\n\\nSo these two elements look the same, but one was styled using an `<h1>` element and the other was styled using a `<div>` element with a class of `header`. Which should you use? Use the element that is **most semantically appropriate**.\\n\\nIf we use a `<div>` it might *look* the same but there\'s no way to tell that this is a header...so for example, Google might not know that this is a header and might not give its content as much weight or prominence in search results. So we should use an `<h1>` element.\\n\\n...but, again, in this specialization, we are not focusing on the many different HTML elements so we might use `<div>` and `<span>` elements in places where you might use other elements. So don\'t worry about it too much. Just know that there are many different HTML elements and that they have different meanings.\\n"]},"Module 3 - Intoduction to Programming with JavaScript":{"01 - Introduction to JavaScript.webnb":["internal","# Introduction to JavaScript\\n\\nSo, we\'ve learned the essentials of HTML, CSS, and the DOM. Now it\'s time to finally focus on the last piece of the puzzle: JavaScript.\\n\\nBut before we dive into JavaScript, I need to give you a quick overview of the history of JavaScript. Because JavaScript has a unique development history that has led to some quirks that might not make sense if you don\'t know the history.\\n"],"02 - A Brief History of JavaScript.webnb":["internal","# A Brief History of JavaScript\\n\\nSo I want you to imagine a really complex piece of software. Like let\'s say MacOS. Now, imagine that you need to:\\n\\n- Support old features forever\\n\x3c!-- You just keep adding new features on top of older ones --\x3e\\n\x3c!-- You\'ll ask questions like \\"why are there like three alternative ways to open this app? --\x3e\\n\x3c!-- Why are there two trash cans and why does one look super old??? ---\x3e\\n- Support code that is decades old\\n- By the way, the first version was implemented in 10 days\\n  - ...by one person\\n\\nBelieve it or not, these are all true of JavaScript. The first version of JavaScript was created back in 1995. It was built to allow web pages to be more like actual applications---dynamic and interactive. One person, Brendan Eich, was tasked to create the first version of JavaScript with fairly loose guidance, except that the syntax should look somewhat like the Java programming language and that it should be beginner friendly. From there, he took some of his favorite features from other languages and created JavaScript over the course of 10 days. Because he ended up taking so many features from other languages, JavaScript is kind of like a platypus of a programming language. It has features from many different languages that don\'t typically go together.\\n\\nThe original idea of JavaScript was that it was going to just be a little scripting glue layer that would connect components written in the \\"real\\" \\"industrial\\" programming languages. Like you have 1000 lines of Java and a tiny bit of JavaScript that decides when that code gets called. But that isn\'t how it ended up...JavaScript ended up being *the* language of the web. And it\'s still the only language that runs in the browser.\\n\\nSo already, between the short time frame, the fact that it was created by one person who borrowed ideas from many languages, and the fact that it was originally intended to be a glue language, JavaScript has some quirks.  But it gets...either worse or better depending on how you look at it. The first version of JavaScript was integrated with the Netscape browser and it quickly caught on. But JavaScript was a proprietary language (meaning that it belonged to Netscape) and when other companies wanted to create their own browsers, they had to create their own versions of JavaScript, which sometimes had subtly different features.\\n\\nBut then, while all this is happening, people are creating actual websites using JavaScript! And if you have a person that creates a website that works in Netscape version 1.0, you can\'t have that website break when someone opens it in Netscape version 2.0 or 3.0....or 20.0. So Netscape had to keep supporting the old features. And then Microsoft came out with Internet Explorer and they had to support the old features. And then other browsers came out and they had to support the old features. And then the web became a thing and now we have to support the old features forever.\\n\\nThat\'s not to say that JavaScript hasn\'t changed since its original release in 1995. It has changed a lot. But it always needs to change in a way that\'s \\"backwards compatible\\", meaning that it won\'t break old websites or features. So when new features get added, they just get added on top of the old features. So for so many things, you\'ll end up with 2 or 3 ways of doing the exact same thing.\\n\\nFor so many things in JavaScript, there\'s usually an \\"old\\" way of doing things but most of the time, that \\"old\\" way of doing things had so many quirks that people came up with a \\"new\\" way of doing things. In this class, we\'re not going to focus on the \\"old\\" way of doing things in JavaScript even though I\'ll be honest---having spent so much time learning about and working with some of the quirky old features, it\'s tempting to want to teach about them in detail. But instead, we\'re going to focus on the \\"new\\" way of doing things. At times, I will mention the \\"old\\" way of doing things, just because some of them are common enough that you\'ll see them in other peoples\' code or documentation.\\n\\nSo now, you have a programming language that was created in 10 days by one person who borrowed ideas from many languages, that was originally intended for a different person, that was fragmented and implemented by many different companies, that has to support old features forever, and that has to support code that is decades old. And that\'s JavaScript.\\n\\nActually, that\'s a bit of a lie. Technically, \\"JavaScript\\" is a trademarked term (now owned by Oracle) and the language that we\'re talking about is actually called ECMAScript. But no one calls it that. So we\'re going to call it JavaScript."],"03 - Adding JavaScript Code.webnb":["internal","# Running JavaScript Code\\n\\nNow that we have some background on JavaScript, of course the next question is how do we actually run JavaScript? Well, there are a few ways.\\n\\n\\nThe easiest way to run JavaScript is to use the console in your browser\'s developer tools. To open the console, right click on a page and select \\"Inspect\\". Then, click on the \\"Console\\" tab. You can type JavaScript into the console and press enter to run it.\\n\\nExample: `1+1`\\n\\n\\nOK...that\'s nice but not very useful. The real question is how do we integrate JavaScript into **our** web pages?\\n\\nThere are three rough options:\\n\\nFirst, we can write limited portions of JavaScript directly into our HTML through attributes like `onclick`.  These little bits of JavaScript will run when the appropriate event occurs. Now, I\'m going to go over this but **we will avoid this in this class** so you can put it in your mind, know that it exists, and then let it fall out of your mind.\\n\\nThere are [many attributes](https://www.w3schools.com/tags/ref_eventattributes.asp) for different events. In the example of `onclick`, the little bit of JavaScript that we write will run when the user clicks on the element.\\n\\nLet\'s see an example:\\n\\n```html\\n<button onclick=\\"alert(\'Hello!\')\\">Click me!</button>\\n```\\n\\nHere, this little bit of code (`alert(\'Hello!\')`) is JavaScript that will run when the user clicks on the button. It calls the `alert()` function with the string `\'Hello!\'` as an argument. This will cause a popup to appear with the text \\"Hello!\\".\\n\\nSo that\'s all I\'m going to say about this kind of inline JavaScript. We will **not** be using it in this class. But, it\'s good to know that it exists.\\n\\nInstead, we\'re going to include JavaScript by using the `<script>` tag. There are two ways to do this:\\n\\nFirst, we can write our code inside of the `<script>` tag. This is called internal JavaScript.\\n\\n```html\\n<script>\\n    console.log(\\"Hello! This is JavaScript code!\\");\\n<\/script>\\n```\\n\\nIn this code, we have a `<script>` tag with some JavaScript code inside of it. This code will run when the browser encounters it. As in wherever the `<script>` tag is in the HTML, the code will run there. This will be important later.\\n\\nAlso, if we want to have multiple `<script>` tags, we can do that. The code in each `<script>` tag will run in the order that the tags appear in the HTML.\\n\\n```html\\n<script>\\n    console.log(\\"Hello! This is JavaScript code!\\");\\n    console.log(\\"...and more\\");\\n<\/script>\\n<script>\\n    console.log(\\"Hello again! This is more JavaScript code!\\");\\n<\/script>\\n```\\n\\nWe can also include JavaScript from an external file. This is called external JavaScript. To do this, we use the `src` attribute of the `<script>` tag. The `src` attribute specifies the path to the JavaScript file that we want to include.\\n\\n```html\\n<script src=\\"script.js\\"><\/script>\\n```\\n\\nHere, we have a `<script>` tag with the `src` attribute set to `\\"script.js\\"`. This will cause the browser to load the file `script.js` and run the code in it.\\n\\nSo, what\'s in `script.js`? Well, it\'s just a JavaScript file. It can contain any JavaScript code that we want. For example:\\n\\n```js\\nconsole.log(\\"Hello! This is JavaScript code!\\");\\nconsole.log(\\"...and more\\");\\n```\\n\\nThis is the same code that we had in the internal JavaScript example. The only difference is that it\'s in a separate file. But the same thing about loading multiple files applies here. If we have multiple `<script>` tags with `src` attributes, the code in each file will run in the order that the tags appear in the HTML.\\n\\n```html\\n<script src=\\"script1.js\\"><\/script>\\n<script src=\\"script2.js\\"><\/script>\\n```\\n\\nscript1.js:\\n```js\\nconsole.log(\\"Hello! This is JavaScript code!\\");\\n```\\n\\nscript2.js:\\n```js\\nconsole.log(\\"...and more\\");\\n```\\n\\nSo, that\'s how we include JavaScript in our web pages. We can either write it directly in the HTML or we can put it in a separate file and include it with the `<script>` tag. Now, let\'s start talking about the actual code that we can write in JavaScript."],"04 - Expressions Values and Types.webnb":["internal","# Expressions, Values, and Types\\n\\nOn a high-level, when programmers write code, they are creating a set of instructions for the computer to follow. These instructions are made up of **expressions**. An expression is a piece of code that follows some \\"syntactic rules\\" (determined by the programming language).\\n\\nAs programmers, our job is to figure out how to write the *expressions* that will get the computer to do what we want it to do. The JavaScript engine will look at all the expressions we write and (if they obey the rules of the language) will execute them. As it runs, it produces a **value** for every expression it evaluates.\\n\\nFor example, the expression:\\n\\n```javascript\\n1 + 2\\n```\\n\\nhas a value of `3`. Every value also has a **type**, which is a way of categorizing what kind of value it is. The value `3` is a number, so its type is `number`.\\no\\nSo in brief:\\n\\n- Programmers write **expressions**.\\n    - These expressions follow the \\"syntactic rules\\" of the programming language.\\n- The JavaScript engine evaluates those expressions and produces a **value**.\\n- Every value has a **type**.\\n\\nA few examples of types in JavaScript are:\\n\\n| Expression | Value | Type |\\n|------------|-------|------|\\n| `1`        | `1`   | `number`  (a type that represents numbers or quantities) |\\n| `1 + 2`    | `3`   | `number` |\\n| \\"Hello\\" | \\"Hello\\" | `string`  (a type that represents text) |\\n| \\"Hello\\" + \\" World\\" | \\"Hello World\\" | `string` |"],"05 - Using the JavaScript console.webnb":["internal","# Using the JavaScript Console\\nThe JavaScript console is an essential tool for web developers. It allows you to interact with the JavaScript engine in your web browser, providing a way to test and debug your code.\\n\\n## Opening the Console\\n\\nTo open the JavaScript console, first visit your webpage in a web browser. If you use Safari, open the \\"Settings\\" -> \\"Advanced\\" -> Make sure \\"Show features for web developers\\" is checked. Then, follow these steps in any browser:\\n\\n- Right-click on the page and select \\"Inspect\\" or \\"Inspect Element\\" from the context menu.\\n- In the Developer Tools panel that opens, look for a tab labeled \\"Console.\\" Click on it to open the console.\\n- You should now see a text area where you can type JavaScript code and execute it.\\n\\n\\n## Logging to the Console\\n\\nAs a first step to learning JavaScript, we\'re going to learn how to log to the console. This is a very useful tool for debugging JavaScript code. It allows us to see what\'s going on in our code without having to change the HTML.\\n\\nNote, however, that **users will not see anything that we log to the console** (unless they open up DevTools). This is only for our benefit as developers.\\n\\nTo log to the console, we use the `console.log()` function. This function takes one argument, which is the thing that we want to log. For example:\\n\\n```javascript\\nconsole.log(\\"Hello! This is JavaScript code!\\");\\n```\\n\\nThis will log the string `\\"Hello! This is JavaScript code!\\"` to the console.\\n\\nThis is kind of the equivalent of Python\'s `print()` function. Except that in Python, we call `print()` whenever we want the user to see something. In JavaScript, most users will *not* see what we log to the console.\\n\\nWe\'ll use `console.log()` a lot It\'s a very useful tool for debugging JavaScript code."],"06 - How JavaScript Runs Code.webnb":["internal","# How JavaScript Runs Code\\n\\nFor the most part, the JavaScript engine runs code in the order that it appears, from top to bottom. This is called **sequential execution**. The engine starts at the top of the code and works its way down, executing each line in order. For example, in the following code:\\n\\n```javascript\\n<html>\\n  <head>\\n    <script>\\n      console.log(\\"Hello\\");\\n      console.log(\\"World\\");\\n    <\/script>\\n  </head>\\n</html>\\n```\\n\\nThe JavaScript engine first executes the line `console.log(\\"Hello\\")`, which logs \\"Hello\\" to the console. Then it moves on to the next line and executes `console.log(\\"World\\")`, logging \\"World\\" to the console. The output will be:\\n\\n```\\nHello\\nWorld\\n```\\n\\nThis also applies if there are multiple `<script>` elements in the HTML file. The JavaScript engine will execute the code in the order that it appears in the HTML file, from top to bottom. For example, if you have two `<script>` elements like this:\\n\\n```html\\n<html>\\n  <head>\\n    <script>\\n      console.log(\\"Hello\\");\\n    <\/script>\\n    <script>\\n      console.log(\\"World\\");\\n    <\/script>\\n  </head>\\n</html>\\n```\\n\\nThis also logs \\"Hello\\" and \\"World\\" to the console in that order."],"07 - Comments in JavaScript.webnb":["internal","# Comments in JavaScript\\n\\nComments tell the JavaScript engine to ignore certain parts of the code. They are useful for adding notes or explanations to your code, making it easier to understand for yourself and others. Comments are mainly used for (1) documenting and/or explaining your code or (2) temporarily disabling code.\\n\\nIn JavaScript, there are two ways to add comments:\\n\\n1. Single-line comments: `// ...`\\n2. Multi-line comments: `/* ... */`\\n\\n## Single-line comments\\nSingle-line comments start with `//` and continue until the end of the line. For example:\\n\\n```javascript\\n// This is a single-line comment\\nconsole.log(\\"Hello, World!\\"); // The console.log is *not* a comment but this text is\\n// console.log(\\"This will not run\\"); // This is a comment\\n```\\n\\nSingle-line comments are useful for adding short explanations or notes to your code. They can be placed on their own line or at the end of a line of code.\\n\\n## Multi-line comments\\n\\nMulti-line comments start with `/*` and end with `*/`. They can span multiple lines. For example:\\n\\n```javascript\\n/* This is a multi-line comment\\n   It can span multiple lines */\\nconsole.log(\\"Hello, World!\\"); /* This is a comment */\\nconsole.log(1 + 2 /* + 3 */); /* logs 1 + 2 (or 3) to the console */\\n```\\n\\nMulti-line comments are useful for adding longer explanations or notes to your code. They can also be used to temporarily disable blocks of code.\\n\\n## Using comments in VS Code\\n\\nWe can use `Ctrl + /` (or `Cmd + /` on a Mac) to add comments to a line or block of code in VS Code. Whichever line or block of code is selected will be commented out (or uncommented) when you press `Ctrl + /`. This is a quick way to add comments to your code without having to type `//` or `/* ... */` manually.\\n"],"08 - Strings and Numbers.webnb":["internal","# Fundamental JavaScript Types: Strings and Numbers\\n\\nTwo of the most fundamental types in JavaScript are strings and numbers. Strings are used to represent text, while numbers are used to represent quantities or values. In this notebook, we\'ll explore how to work with these two types in JavaScript.\\n\\n## Strings\\n\\nStrings are used to represent text in JavaScript. They can be created using single quotes (`\'`), double quotes (`\\"`), or backticks (`` ` ``). For example:\\n\\n```javascript\\n\'Hello, World!\'\\n```\\n\\ncreates a string using single quotes, while:\\n\\n```javascript\\n\\"Hello, World!\\"\\n```\\n\\nCreates the same string using double quotes. Either way is valid, as long as you start and end the string with the same type of quote. For example, this is invalid:\\n\\n```javascript\\n\'Hello, World!\\"\\n```\\n\\nThis will result in a syntax error because the string is not properly closed. You might want to use double quotes when the string contains single quotes, or vice versa. For example:\\n\\n```javascript\\n\'Steve\'s car\'\\n```\\n\\nis not valid because the single quote in \\"Steve\'s\\" closes the string. To fix this, you can use double quotes:\\n\\n```javascript\\n\\"Steve\'s car\\"\\n```\\n\\nor\\n\\n```javascript\\n\'Steve said \\"Hello\\"\'\\n```\\n\\nIf you want to include the same type of quote inside the string, you can \\"escape\\" it using a backslash (`\\\\`). For example:\\n\\n```javascript\\n\'Steve\\\\\'s car\'\\n```\\n\\n## Numbers\\nNumbers are used to represent quantities or values in JavaScript. They can be integers (whole numbers) or floating-point numbers (numbers with decimal points). For example:\\n\\n```javascript\\n1\\n```\\n\\n```javascript\\n1.5\\n```\\n\\n```javascript\\n-1\\n```\\n\\nAll of these expressions are \\"literal\\" expressions, meaning the value is the same as the expression."],"09 - Arithmetic Operators.webnb":["internal","# Arithmetic Operators\\n\\nWe can use **arithmetic operators** to perform mathematical operations on numbers. The most common arithmetic operators are:\\n- `+` (addition)\\n- `-` (subtraction)\\n- `*` (multiplication)\\n- `/` (division)\\n\\n\\nWe can also use `-` to negate a number (making it negative).\\n\\n\\nThe expression `1 + 2` has a value of `3`, and the expression `1 - 2` has a value of `-1`. The expression `1 * 2` has a value of `2`, and the expression `1 / 2` has a value of `0.5`. The expression `-1` has a value of `-1`.\\n\\nWhen the JavaScript engine evaluates arithmetic expressions, it follows the order of operations (also known as operator precedence). The order of operations is:\\n\\n1. Parentheses `()`\\n2. Multiplication `*` and division `/`\\n3. Addition `+` and subtraction `-`\\n\\nFor example, if we wanted to take the average of two numbers, we would add them together and divide by `2`:\\n```javascript\\n(10 + 20) / 2\\n```\\n\\nif we tried to do this:\\n```javascript\\n10 + 20 / 2\\n```\\n\\nit would be evaluated as:\\n```javascript\\n10 + (20 / 2)\\n```\\nThis is because division has a higher precedence than addition. So the expression would be evaluated as `10 + 10`, which is `20`."],"10 - Defining Variables.webnb":["internal","# Defining Variables\\n\\nJavaScript requires you to **declare** variables before you can use them. So if you want to use a variable, you need to declare that it exists. There are two ways to do this: using `let` or `const`.\\n\\nHere\'s an example of how to declare a variable using `let`:\\n\\n```javascript\\nlet x;\\nx = 10;\\n```\\n\\nThe **first** line (`let x`) declares that the variable `x` exists. The **second** line (`x = 10`) assigns the value `10` to the variable `x`. Here, we declare and assign the variable `x` in two separate lines, but we normally do it in one line:\\n\\n```javascript\\nlet x = 10;\\n```\\n\\nIf we want to re-assign the variable `x`, we can just do it *without* declaring it again:\\n\\n```javascript\\nlet x = 10;\\nx = 20;\\n```\\n\\nThen, when we want to use the variable `x`, we can just use it:\\n\\n```javascript\\nlet x = 10;\\nx = 20;\\nconsole.log(x);\\n```\\n\\nThere are three ways to declare variables. There\'s an old fashioned way that I won\'t get into right now and then there are the two more modern ways: `let` and `const`. `let` allows you to reassign the variable, while `const` makes the variable a constant (cannot be reassigned).\\n\\n```javascript\\nlet x = 10;\\nx = 20;\\nconsole.log(x);\\nconst y = 10;\\ny = 20; // ERROR: Cannot reassign a constant\\nconsole.log(y);\\n```\\n\\nNow, there\'s also a third way to declare variables, using the `var` keyword. This is an older way to declare variables, and it has some weird behavior, so we won\'t use it. But if you see it in other people\'s code, you should know what it is.\\n\\n```javascript\\nlet x = 10;\\nx = 20;\\nconsole.log(x);\\nconst y = 10;\\ny = 20; // ERROR: Cannot reassign a constant\\nconsole.log(y);\\nvar z = x + y;\\n```\\n\\n(rename `var` to `const`).\\n\\nSo there are two ways to declare variables: `let` and `const`.\\n\\nThis might lead to two questions:\\n\\n1. What happens if we forget to declare a variable?\\n2. When should we use `let` and when should we use `const`?\\n\\n1. If you forget to declare a variable, everything will still work...or really, it will seem to work but you are likely get some weird behavior...this isn\'t something that you\'d rarely see (like forgetting to declare the DOCTYPE in HTML)...this is something where you\'ll *probably* run into some hard to debug problem if you don\'t declare your variables. It won\'t happen every time or even most of the time but it will happen. So you should always declare your variables.\\n\\n2. Which one should you use? Well, you should use `const` whenever you can, and only use `let` when you need to reassign the variable.\\n\\nSo I *always* declare my variables using `const` until I know I\'ll need to reassign the variable, and only then I switch to `let`. This is a good habit to get into.\\n\\nYou may ask...wait a second...why should we ever use `const` when `let` allows us to do everything `const` does and more? Well permisiveness is not always a good thing.\\n\\nBeing less permissive can actually make `const` safer than `let`. If you use `const`, you can\'t accidentally reassign the variable. If you use `let`, you can accidentally reassign the variable. So if you use `const`, you can avoid making mistakes.\\n\\n## Variable Naming\\n\\nWhen you declare a variable, you need to give it a name. This is called the variable name. The variable name can be anything you want, but there are some rules you need to follow:\\n1. The variable name must start with a letter, underscore (`_`), or dollar sign (`$`).\\n2. The variable name can **only** contain letters, numbers, underscores (`_`), and dollar signs (`$`).\\n\\n\\n| Variable Name | Valid? |\\n| -------------- | ------ |\\n| `x`            | Yes    |\\n| `x1`           | Yes    |\\n| `x_1`          | Yes    |\\n| `x$1`          | Yes    |\\n| `1x`           | No (starts with a number) |\\n| `x 1`          | No (contains a space) |"],"11 - Referencing Variables.webnb":["internal","# Referencing Variables\\n\\nTo reference a variable, you simply use its name. For example, if you have a variable `x` that holds the value `10`, you can reference it like this:\\n```javascript\\nlet x = 10;\\n\\nconsole.log(x); // Output: 10\\n```\\n\\nThis will log the value of `x` to the console, which is `10`. You can use the variable in expressions, like this:\\n\\n\\nOne thing that is important to understand is the difference between variable reference expressions and string literals. A variable reference expression is an expression that evaluates to a variable, while a string literal is a string that is hard-coded into the program. For example:\\n\\n```javascript\\n\\nlet x = 10;\\nconsole.log(x); // Output: 10\\nconsole.log(\\"x\\"); // Output: \\"x\\"\\n```\\n\\nIn the first `console.log`, we are referencing the variable `x`, which evaluates to `10`. In the second `console.log`, we are using a string literal `\\"x\\"`, which is just a string and does not evaluate to anything. It will print the string `\\"x\\"` to the console."],"12 - Booleans and Comparison Operators.webnb":["internal","# Booleans and Comparison Operators\\n\\n**Booleans** are a JavaScript type that represents whether something is *true* or *false*. Unlike most other types, booleans can only have one of two values: `true` or `false`. For example, we could assign a boolean value to a variable like this:\\n```javascript\\nlet isTrue = true;\\nlet isFalse = false;\\n```\\n\\nHowever, we almost never use boolean literals (`true` or `false`) directly. Instead, we use **comparison operators** to compare values and return a boolean value. The most common comparison operators are:\\n\\n- `===` (equal to)\\n- `!==` (not equal to)\\n- `>` (greater than)\\n- `<` (less than)\\n- `>=` (greater than or equal to)\\n- `<=` (less than or equal to)\\n\\nFor example, the expression `1 === 1` evaluates to `true`, while the expression `1 === 2` evaluates to `false`. Similarly, the expression `1 > 2` evaluates to `false`, while the expression `2 > 1` evaluates to `true`.\\n\\n\\nFor a few examples:\\n\\n| Expression | Value |\\n|------------|-------|\\n| `1 === 1` | `true` |\\n| `1 === 2` | `false` |\\n| `1 > 2` | `false` |\\n| `2 > 1` | `true` |\\n| `1 !== 2` | `true` |\\n| `1 !== 1` | `false` |\\n\\n\\nWe can also use comparison operators to compare strings. For example, the expression `\\"a\\" === \\"a\\"` evaluates to `true`, while the expression `\\"a\\" === \\"b\\"` evaluates to `false`.\\n\\n\\nNote that it does not matter how we create the values that we compare. For example, `1+2 === 3` evaluates to `true` because both expressions (`1+2` and `3`) evaluate to the same value (`3`). Similarly, `\'Hello\' === \\"Hello\\"` evaluates to `true` because both expressions evaluate to the same string value (`\\"Hello\\"`), even if one is created with double quotes and the other with single quotes."],"13 - Logical Operators.webnb":["internal","# Logical Operators\\n\\n**Logical operators** allow us to combine boolean values and expressions to create more complex conditions. The most common logical operators are:\\n\\n- `&&` (logical AND)\\n- `||` (logical OR)\\n- `!` (logical NOT)\\n\\n\\n## Logical AND (`&&`)\\nThe logical AND operator (`&&`) returns `true` if both operands are `true`, and `false` otherwise. The following table summarizes the behavior of the logical AND operator:\\n\\n| Operand 1 | Operand 2 | Result |\\n|-----------|-----------|--------|\\n| `true`    | `true`    | `true` |\\n| `true`    | `false`   | `false` |\\n| `false`   | `true`    | `false` |\\n| `false`   | `false`   | `false` |\\n\\nFor example, the expression `true && true` evaluates to `true`, while the expression `true && false` evaluates to `false`. Similarly, the expression `false && true` evaluates to `false`, and the expression `false && false` evaluates to `false`.\\n\\n\\n## Logical OR (`||`)\\n\\nThe logical OR operator (`||`) returns `true` if at least one of the operands is `true`, and `false` if both operands are `false`. The following table summarizes the behavior of the logical OR operator:\\n\\n| Operand 1 | Operand 2 | Result |\\n|-----------|-----------|--------|\\n| `true`    | `true`    | `true` |\\n| `true`    | `false`   | `true` |\\n| `false`   | `true`    | `true` |\\n| `false`   | `false`   | `false` |\\n\\nFor example, the expression `true || true` evaluates to `true`, while the expression `true || false` evaluates to `true`. Similarly, the expression `false || true` evaluates to `true`, and the expression `false || false` evaluates to `false`.\\n\\n## Logical NOT (`!`)\\n\\nThe logical NOT operator (`!`) negates a boolean value. It returns `true` if the operand is `false`, and `false` if the operand is `true`. The following table summarizes the behavior of the logical NOT operator:\\n| Operand | Result |\\n|---------|--------|\\n| `true`  | `false` |\\n| `false` | `true`  |\\n\\nFor example, the expression `!true` evaluates to `false`, while the expression `!false` evaluates to `true`. Similarly, the expression `!(1 === 1)` evaluates to `false`, and the expression `!(1 === 2)` evaluates to `true`."],"14 - Conditional Statements.webnb":["internal","# Conditional Statements\\n\\nSo we\'ve talked about how to produce boolean values using comparison operators and logical operators. But what can we actually do with them? We can use boolean expressions to decide which code to run. This is called **conditional execution**. In JavaScript, we can use the `if` statement to execute code conditionally. The basic syntax of an `if` statement is:\\n```javascript\\nif (condition) {\\n  // code to execute if condition is true\\n}\\n```\\n\\nThe `condition` is a boolean expression that evaluates to either `true` or `false`. If the condition is `true`, the code inside the curly braces `{}` is executed. If the condition is `false`, the code inside the curly braces is skipped.\\n\\n\\nFor example, we can use an `if` statement to check if a number is positive:\\n```javascript\\nlet number = 5;\\nif (number > 0) {\\n  console.log(\\"The number is positive.\\");\\n}\\n```\\nIn this example, the condition `number > 0` evaluates to `true`, so the code inside the `if` statement is executed, and \\"The number is positive.\\" is logged to the console.\\n\\n\\nWe can also use `else` to specify code to run if the condition is `false`. The syntax for an `if...else` statement is:\\n```javascript\\nif (condition) {\\n  // code to execute if condition is true\\n} else {\\n  // code to execute if condition is false\\n}\\n```\\nFor example, we can use an `if...else` statement to check if a number is positive or negative:\\n```javascript\\nlet number = -5;\\nif (number > 0) {\\n  console.log(\\"The number is positive.\\");\\n} else {\\n  console.log(\\"The number is negative.\\");\\n}\\n```\\n\\nIn this example, the condition `number > 0` evaluates to `false`, so the code inside the `else` statement is executed, and \\"The number is negative.\\" is logged to the console.\\n\\nWe can also use `else if` to check multiple conditions. The syntax for an `if...else if...else` statement is:\\n```javascript\\nif (condition1) {\\n  // code to execute if condition1 is true\\n} else if (condition2) {\\n  // code to execute if condition2 is true\\n} else {\\n  // code to execute if both conditions are false\\n}\\n```\\nFor example, we can use an `if...else if...else` statement to check if a number is positive, negative, or zero:\\n```javascript\\nlet number = 0;\\n\\nif (number > 0) {\\n  console.log(\\"The number is positive.\\");\\n} else if (number < 0) {\\n  console.log(\\"The number is negative.\\");\\n} else {\\n  console.log(\\"The number is zero.\\");\\n}\\n```\\nIn this example, the condition `number > 0` evaluates to `false`, the condition `number < 0` also evaluates to `false`, so the code inside the `else` statement is executed, and \\"The number is zero.\\" is logged to the console.\\n"],"15 - Array Fundamentals.webnb":["internal","# Array Fundamentals\\n\\n**Arrays** are an important JavaScript type. They can represent sequences of values. For example, we might use an array of numbers to represent a list of scores, or an array of strings to represent a list of names. In JavaScript, arrays are created using square brackets (`[]`). For example:\\n```javascript\\nlet scores = [10, 20, 30, 40, 50];\\n```\\n\\n\\nIn this example, we create an array called `scores` that contains five numbers. Arrays are \\"zero-indexed\\", which means that the first element of the array is at index `0`, the second element is at index `1`, and so on. To access an individual element of the array, we use the index in square brackets. For example:\\n\\n```javascript\\nconsole.log(scores[0]); // Output: 10\\nconsole.log(scores[1]); // Output: 20\\nconsole.log(scores[2]); // Output: 30\\nconsole.log(scores[3]); // Output: 40\\nconsole.log(scores[4]); // Output: 50\\n```\\n\\nIf we try to access an index that is out of bounds (for example, `scores[5]` or `scores[100]`), we will get `undefined` because there is no element at that index.\\n\\n\\nWe can also use the `length` property of the array to get the number of elements in the array. For example:\\n```javascript\\nconsole.log(scores.length); // Output: 5\\n```\\n\\n\\nThe last item in the array is at index `length - 1`. For example, the last item in the `scores` array is at index `4`, which is `scores.length - 1`."],"16 - while loops.webnb":["internal","# `while` loops\\n\\nFor the most part, the JavaScript engine runs code in the order that it appears, from top to bottom. As we found, conditional statements like `if` are an exception to this rule. Another exception is the `while` loop. A `while` loop is a way to repeat a block of code as long as a certain condition is true. The basic syntax of a `while` loop is:\\n```javascript\\nwhile (condition) {\\n  // code to execute while condition is true\\n}\\n```\\n\\nA `while` loop is a bit like an `if` statement, in that it checks if a condition is `true` to decide whether to run the code in the curly braces (`{}`). The difference is that when a `while` loop runs, it goes back to the top and checks the condition again. If the condition is still `true`, it runs the code in the curly braces again. This continues until the condition is `false`, at which point the loop stops running.\\n\\nFor example, we can use a `while` loop to print the numbers from 1 to 5:\\n```javascript\\nlet number = 1;\\nwhile (number <= 5) {\\n  console.log(number);\\n  number++; // Increment the number by 1\\n}\\n```\\nIn this code, `number++` is a shorthand way of saying `number = number + 1`. The loop starts with `number` equal to `1`, and it continues to run as long as `number` is less than or equal to `5`. Each time the loop runs, it prints the current value of `number` and then increments it by `1`. When `number` becomes `6`, the condition `number <= 5` is no longer true, so the loop stops running. The output will be:\\n\\n\\n```\\n1\\n2\\n3\\n4\\n5\\n```\\n\\nWe can also use a `while` loop to loop through an array. For example, we can use a `while` loop to print each element of an array:\\n```javascript\\nlet scores = [10, 20, 30, 40, 50];\\nlet index = 0;\\n\\nwhile (index < scores.length) {\\n  console.log(scores[index]);\\n  index++; // Increment the index by 1\\n}\\n```\\nIn this code, we start with `index` equal to `0`, and the loop continues to run as long as `index` is less than the length of the `scores` array. Each time the loop runs, it prints the current element of the array and then increments `index` by `1`. When `index` becomes equal to the length of the array, the condition `index < scores.length` is no longer true, so the loop stops running. The output will be:\\n\\n```\\n10\\n20\\n30\\n40\\n50\\n```\\n\\n## Infinite Loops\\n\\nOne thing to be careful of when using `while` loops is that if the condition never becomes `false`, the loop will run forever. This is called an **infinite loop**. For example, if we forget to increment `number` in the first example, the loop will run forever because `number` will always be `1`, and the condition `number <= 5` will always be `true`:\\n```javascript\\nlet number = 1;\\nwhile (number <= 5) {\\n  console.log(number);\\n  // number++; // This line is missing, creating an infinite loop\\n}\\n```"],"17 - for loops.webnb":["internal","# `for` loops\\n\\nIn the previous section, we learned about `while` loops, which are used to repeat a block of code as long as a certain condition is true. There are three common steps to a `while` loop:\\n\\n1. Initialize a variable (e.g., `let number = 1;`)\\n2. Check the condition (e.g., `number <= 5`)\\n3. Update the variable (e.g., `number++`)\\n\\nFor example, we can use a `while` loop to print the numbers from 1 to 5:\\n```javascript\\nlet number = 1; // Initialize the variable\\n\\nwhile (number <= 5) { // Check the condition\\n  console.log(number);\\n  number++; // Update the variable\\n}\\n```\\n\\nThe `for` loop is a more concise way to write a loop that has these three steps. The basic syntax of a `for` loop is:\\n```javascript\\nfor (initialization; condition; update) {\\n  // code to execute while condition is true\\n}\\n```\\nIn this syntax:\\n- `initialization` is where we initialize the variable (e.g., `let number = 1;`)\\n    - This is done only once at the beginning of the loop\\n- `condition` is the condition that is checked before each iteration (e.g., `number <= 5`)\\n    - If the condition is `true`, the code inside the loop is executed\\n    - If the condition is `false`, the loop stops running\\n- `update` is where we update the variable (e.g., `number++`)\\n    - This is done at the end of each iteration of the loop\\n\\nFor example, we can use a `for` loop to print the numbers from 1 to 5:\\n```javascript\\nfor (let number = 1; number <= 5; number++) {\\n  console.log(number);\\n}\\n```\\n\\nThis is equivalent to the following `while` loop:\\n```javascript\\nlet number = 1; // Initialize the variable\\nwhile (number <= 5) { // Check the condition\\n  console.log(number);\\n  number++; // Update the variable\\n}\\n```\\n\\n\\nAnother example of a `for` loop is to loop through an array. For example, we can use a `for` loop to print each element of an array:\\n```javascript\\nlet scores = [10, 20, 30, 40, 50];\\nfor (let index = 0; index < scores.length; index++) {\\n  console.log(scores[index]);\\n}\\n```\\nThis is equivalent to the following `while` loop:\\n```javascript\\nlet scores = [10, 20, 30, 40, 50];\\nlet index = 0; // Initialize the variable\\nwhile (index < scores.length) { // Check the condition\\n  console.log(scores[index]);\\n  index++; // Update the variable\\n}\\n```\\n\\n\\nIf we want to loop through from *end* to *start*, we can do that too. For example, we can use a `for` loop to print each element of an array in reverse order:\\n```javascript\\nlet scores = [10, 20, 30, 40, 50];\\nfor (let index = scores.length - 1; index >= 0; index--) {\\n  console.log(scores[index]);\\n}\\n```"],"18 - for of loops.webnb":["internal","# `for...of` loops\\n\\nThe `for...of` loop is a special type of `for` loop that is used to iterate over iterable objects, such as arrays, strings, and other collections. It allows you to loop through the values of an iterable without needing to use an index. It\'s a little misleading to call it a \\"for\\" loop, as it is not a traditional `for` loop. Instead, it is a more modern way to iterate over collections in JavaScript.\\n\\n The basic syntax of a `for...of` loop is:\\n```javascript\\nfor (const value of iterable) {\\n  // code to execute for each value\\n}\\n```\\n\\nIn this syntax:\\n- `iterable` is the collection you want to iterate over (e.g., an array or a string).\\n- `value` is a variable that will hold the current value of the iterable during each iteration of the loop.\\n- The code inside the curly braces (`{}`) will be executed for each value in the iterable.\\n\\n\\nFor example, we can use a `for...of` loop to print each element of an array:\\n```javascript\\nconst scores = [10, 20, 30, 40, 50];\\nfor (const score of scores) {\\n  console.log(score);\\n}\\n```\\nIn this code, `score` is a variable that holds the current value of the `scores` array during each iteration of the loop. The loop will print each score in the array."],"19 - What are functions.webnb":["internal","# What are Functions\\n\\nFunctions are a way to group code together so that we can use it multiple times. They are like a box that we can put code in, and then we can use that box whenever we want. Functions are a fundamental part of programming, and they allow us to write code that is more organized and easier to read.\\n\\n\\nWe will use these boxes to represent functions:\\n\\n![](function_object.png)\\n![](function_single_arg.png)\\n\\n\\nThusfar, we have been **calling** functions. For example, we have been using the `console.log` function to log things to the console. Now, let\'s learn how to **define** them.\\n"],"20 - Defining functions using the function keyword.webnb":["internal","# Defining Functions\\n\\n\\nWe can create a new function using the `function` keyword. The syntax for defining a function is:\\n```javascript\\nfunction functionName() {\\n  // code to execute\\n}\\n```\\n\\nIn this syntax, `functionName` is the name of the function. It should be a valid JavaScript identifier (e.g., it cannot start with a number).\\nThe code inside the curly braces `{}` is the body of the function. This code will be executed when the function is called (also known as being \\"invoked\\").\\n\\n\\nFor example, the following code defines a function named `sayHello` that prints \\"Hello, world!\\" to the console:\\n```javascript\\nfunction sayHello() {\\n  console.log(\\"Hello, world!\\");\\n}\\n```\\n\\nNote that when we execute this code, nothing gets logged to the console. This is because we have only defined the function, but we have not called it yet. We will talk more about calling functions in the next section."],"21 - Invoking functions.webnb":["internal","# Invoking Functions\\n\\nInvoking (or calling) a function will execute the code inside the function definition. We can invoke a function by using its name followed by parentheses `()`. For example, if we have a function named `myFunction`, we can invoke it like this:\\n```javascript\\nmyFunction();\\n```\\nThis will execute the code inside the `myFunction` definition.\\n\\n\\nFor example, if we have defined a function named `sayHello` that prints \\"Hello, world!\\" to the console:\\n```javascript\\nfunction sayHello() {\\n  console.log(\\"Hello, world!\\");\\n}\\n```\\nWe can invoke it like this:\\n```javascript\\nsayHello();\\n```\\nThis will log \\"Hello, world!\\" to the console.\\n\\n## Invoking vs. Referencing Functions\\n\\nAdding parentheses `()` after a function name invokes the function, while omitting them references the function. For example:\\n```javascript\\nfunction myFunction() {\\n  console.log(\\"Hello, world!\\");\\n}\\nconsole.log(myFunction); // Output: [Function: myFunction]\\nconsole.log(myFunction()); // Output: Hello, world!\\n```\\n\\nThe difference is that `myFunction` without parentheses references the function itself, while `myFunction()` with parentheses invokes the function and executes its code."],"22 - Function arguments and parameters.webnb":["internal","# Function Arguments and Parameters\\n\\nFunction parameters allow us to customize the behavior of functions by passing in values when we call them. This is a powerful feature of JavaScript that allows us to create more flexible and reusable code.\\n\\nLast time, we talked about how to define and call functions in JavaScript. But sometimes we want to pass additional information into a function to customize how it gets called. We do that with *arguments*. For example, if we wanted `myFunction()` to greet a specific person, we could pass in their name as an argument:\\n\\n```js\\nfunction myFunction (name) {\\n    console.log(\\"Hello \\" + name + \\"!\\");\\n    console.log(\\"You Called the function\\");\\n};\\n\\nmyFunction(\\"Evelyn\\");\\nmyFunction(\\"Fei\\");\\nmyFunction(\\"Grace\\");\\n```\\n\\nHere, we\'ve added a parameter called `name` to the function definition. We can then use that parameter inside the function body. When we call the function, we pass in the value that we want to use for the parameter. So when we call `myFunction(\\"Evelyn\\")`, the value `\\"Evelyn\\"` gets assigned to the `name` parameter but just during that function call. So the first time we call the function, the value of `name` is `\\"Evelyn\\"`. The second time we call the function, the value of `name` is `\\"Fei\\"`. And the third time we call the function, the value of `name` is `\\"Grace\\"`.\\n\\n\\nWe can have any number of arguments, separated by commas. So for example, suppose we wanted to instead define a function to add two numbers together. We could do this:\\n\\n```js\\nfunction add(a, b)  {\\n    console.log(a + b);\\n};\\n\\nadd(1, 2);\\nadd(3, 4);\\n```\\n\\nHere, our `add()` function accepts two arguments, `a` and `b`, and logs their sum.\\n\\n.....note though that it **logs** the sum, it doesn\'t **return** the sum. What that means is that we might we the correct \\"answer\\" in the console but the value of the expression `add(1, 2)` is `undefined` (which is like Python\'s `None`). So if I say `const result = add(1, 2)`, the value of `result` is `undefined`. So if we want to return a value from a function, we need to use the `return` keyword:\\n\\n```js\\nfunction add (a, b) {\\n    return a + b;\\n};\\n\\nconst result = add(1, 2);\\nconsole.log(result); // 3\\n```\\n\\nSo that\'s how we can define functions that accept arguments and return values. Next, let\'s talk about how to use functions as arguments to other functions."],"23 - The return statement.webnb":["internal","# The `return` Statement\\n\\nEvery function call is an expression, meaning that it has a value. For example, the expression `add(1,2)` is a value. The value of a function call will be **whatever the function returns**. The `return` statement is how we specify what value a function should return. For example, in the following code:\\n```javascript\\nfunction add(x, y) {\\n  return x + y;\\n}\\n```\\nThe `add` function takes two arguments, `x` and `y`, and returns their sum. So the expression `add(1, 2)` has a value of `3`. The `return` statement is used to specify what value the function should return when it is called.\\nThe `return` statement can also be used to exit a function early. For example, in the following code:\\n```javascript\\nfunction add(x, y) {\\n  if (x < 0 || y < 0) {\\n    return \\"Invalid input\\";\\n  }\\n\\n  return x + y; // This line will not be executed if the input is invalid\\n}\\n```"],"function_object.png":["external",2955],"function_single_arg.png":["external",61900]},"Module 4 - JavaScript Fundamentals":{"01 - JavaScript Fundamentals Intro.webnb":["internal","# JavaScript Fundamentals\\n\\nNow, we\'ve covered some of the programming fundamentals and it is time to dive into JavaScript."],"02 - JavaScript Objects.webnb":["internal","# JavaScript Objects\\n\\nJavaScript objects are a way to group related data and functionality together.\\n\\nWe can create an object using curly braces `{}`. For example:\\n```javascript\\nconst person = {\\n  name: \\"Alice\\",\\n  age: 30\\n};\\n```\\n\\nWithin these curly braces are key-value pairs known as properties. We can look up the value of any key in the object. Each key is a string, and the value can be any JavaScript value (including other objects).\\n\\nValues can also be functions. These are called methods. For example:\\n```javascript\\nconst person = {\\n  name: \\"Alice\\",\\n  age: 30,\\n  greet: function() {\\n    console.log(\\"Hello, my name is \\" + this.name);\\n  }\\n};\\n```\\n\\nSo a method is a function that \\"belongs\\" to an object. We can call the method using dot notation:\\n\\n\\nTo access the value of a property, we use `objectName.propertyName`. For example:\\n```javascript\\nconsole.log(person.name); // Output: Alice\\nconsole.log(person.age); // Output: 30\\n```\\n\\nWe can also call methods using dot notation:\\n```javascript\\nperson.greet(); // Output: Hello, my name is Alice\\n```\\n\\nValues can also be other objects. For example:\\n```javascript\\nconst person = {\\n  name: \\"Alice\\",\\n  age: 30,\\n  address: {\\n    street: \\"123 Main St\\",\\n    city: \\"New York\\"\\n  }\\n};\\n```\\nWe can access the properties of nested objects using dot notation:\\n```javascript\\nconsole.log(person.address.street); // Output: 123 Main St\\nconsole.log(person.address.city); // Output: New York\\n```\\n\\nWe can also use bracket notation to access properties. This is useful when the property name is not a valid JavaScript identifier (e.g., it contains spaces or special characters):\\n```javascript\\nconsole.log(person[\\"name\\"]); // Output: Alice\\nconsole.log(person[\\"age\\"]); // Output: 30\\n```"],"03 - Built-in Objects: window and document.webnb":["internal","# Built-in Objects: `window` and `document`\\n\\nJavaScript has several built-in objects that provide functionality for web development. Two of the most important ones are `window` and `document`.\\n\\n## `window`\\n\\nThe `window` object represents the browser window or tab. It is the global object in JavaScript, meaning that all global variables and functions are properties of the `window` object. It has methods like:\\n\\n- `alert()`: Displays an alert dialog with a message.\\n- `prompt()`: Displays a dialog that prompts the user for input.\\n\\nand properties like:\\n\\n- `location`: Represents the current URL of the window.\\n- `innerWidth`: The width of the window\'s content area.\\n- `innerHeight`: The height of the window\'s content area.\\n\\nBut because `window` is the global object, we can access all of its properties and methods without prefixing them with `window.`. For example, we can call `alert()` directly instead of `window.alert()`. This is a convenience feature of JavaScript.\\n\\n## `document`\\n\\nThe `document` object represents the HTML document that is currently loaded in the browser. It provides methods and properties for manipulating the content and structure of the document. We will talk about it more in the next section."],"04 - DOM Elements in JavaScript.webnb":["internal","# DOM Elements in JavaScript\\n\\nBefore we can get to JavaScript, we need to talk about the DOM because it\'s at the center of how we write JavaScript that changes the page.\\n\\n- The DOM is a **tree** that represents the structure of a web page\\n- The DOM is **dynamic** and **mutable**\\n    - We can **change** the DOM\\n    - We can **add** and **remove** elements\\n- When we write HTML, we are **creating** the DOM\\n    - ...by the way, CSS creates the CSSOM....but no need to talk about that\\n\\n\\n## The DOM (**D**ocument **O**bject **M**odel)\\n\\n![](dom_tree.png)\\n\\n- The DOM is what we **see** and **interact with**\\n- HTML is what we write to **create** the DOM\\n- CSS is what we write to **style** the DOM\\n- JavaScript (will be) what we write to **change** the DOM\\n\\n![](DOM%20JS%20CSSOM.png)\\n\\n\\n## How do we see the DOM?\\n\\n- Developer tools!\\n- Right click on a page and select \\"Inspect\\"\\n\\n## The `document` object\\n\\nThe `document` object is the root of the DOM tree. It represents the entire HTML document. It is the entry point to the DOM tree. We can use it to access any element in the DOM tree.\\n\\n\\n- `document.querySelector()` returns the **first** element that matches the selector\\n- `document.querySelectorAll()` returns **all** elements that match the selector\\n\\n- ...there are other (older) methods but we won\'t use them\\n\\n- We can **change** the DOM using JavaScript\\n\\n```javascript\\nconst p = document.querySelector(\'p\');\\np.innerHTML = \'This is a new paragraph\';\\n```"],"05 - Listening for Click Events.webnb":["internal","# Listening for Click Events\\n\\nTo add an event listener, we need to first get a reference to the element we want to listen to. We can use `document.querySelector()` to get a reference to the first element that matches a CSS selector. For example, if we have a button with an ID of `my-button`, we can get a reference to it like this:\\n\\n```js\\nconst myButton = document.querySelector(\'#my-button\');\\n```\\n\\nOnce we have a reference to the element, we can call `addEventListener()` on it. The first argument is the name of the event we want to listen to, and the second argument is the callback we want to run when the event occurs. For example, if we want to run a callback when the user clicks the button, we can do this:\\n\\n```js\\nmyButton.addEventListener(\'click\', () => {\\n  console.log(\'The button was clicked!\');\\n});\\n```"],"06 - Call Stacks.webnb":["internal","# Call Stacks\\n\\nA **call stack** is a data structure that keeps track of function calls in a program. It works like a stack of plates: you can only add or remove the top plate. When a function is called, it is added to the top of the stack, and when it returns, it is removed from the top of the stack.\\n\\nThe call stack is used to keep track of the order in which functions are called and to manage the flow of control in a program. When a function is called, it is added to the top of the stack, and when it returns, it is removed from the top of the stack. This allows the JavaScript engine to keep track of which function is currently executing and where to return when a function call is complete.\\n\\n\\n## Example\\n\\n```javascript\\nfunction first() {\\n  console.log(\\"First function\\");\\n  second();\\n  console.log(\\"Back to first function\\");\\n}\\nfunction second() {\\n  console.log(\\"Second function\\");\\n  third();\\n  console.log(\\"Back to second function\\");\\n}\\nfunction third() {\\n  console.log(\\"Third function\\");\\n}\\nfirst();\\n```\\n### Output\\n\\n```\\nFirst function\\nSecond function\\nThird function\\nBack to second function\\nBack to first function\\n```"],"07 - Breakpoints.webnb":["internal","# Breakpoints\\n\\nA **breakpoint** is a marker that you can set in your code to pause the execution of the program at a specific line. This allows you to inspect the state of the program at that point, including the values of variables and the call stack.\\nYou can set breakpoints in your code using the developer tools in your web browser. When the program reaches a breakpoint, it will pause execution and allow you to inspect the state of the program. You can then step through the code line by line\\n"],"08 - Debugging Strategies.webnb":["internal","# Debugging Strategies\\n\\n1. **Use the Console**: The console is a powerful tool for debugging JavaScript code. You can use `console.log()` to print values and messages to the console, which can help you understand what your code is doing at different points in time.\\n2. **Use Breakpoints**: Breakpoints allow you to pause the execution of your code at a specific line. This lets you inspect the values of variables and the state of your program at that point in time. You can set breakpoints in your code using the Developer Tools in your web browser.\\n3. **Use the Debugger**: The debugger is a built-in tool in most web browsers that allows you to step through your code line by line. This can help you understand how your code is executing and identify any issues.\\n"],"09 - URL Components.webnb":["internal","# URL Components\\n\\nWhen we see a URL, we can break it down into its components. This is important because we can use these components to build URLs dynamically.\\n\\n## URL Components\\n- **Protocol**: The protocol is the method used to access the resource. The most common protocols are `http` and `https`. The protocol is followed by `://`.\\n- **Domain**: The domain is the name of the server that hosts the resource. It is usually a human-readable name, such as `www.example.com`.\\n- **Path**: The path is the location of the resource on the server. It is usually a series of directories and subdirectories, separated by `/`.\\n- **Query String**: The query string is a set of key-value pairs that provide additional information to the server. It is usually preceded by a `?` and separated by `&`.\\n- **Fragment**: The fragment is a specific location within the resource. It is usually preceded by a `#`.\\n\\n## Example URL\\n```\\nhttps://www.example.com/path/to/resource?key1=value1&key2=value2#fragment\\n```\\n- **Protocol**: `https`\\n- **Domain**: `www.example.com`\\n- **Path**: `/path/to/resource`\\n- **Query String**: `key1=value1&key2=value2`\\n- **Fragment**: `fragment`"],"10 - Paths.webnb":["internal","# Paths\\n\\n- `.` - Current directory\\n- `..` - Parent directory\\n- `/` - Root directory\\n\\nPaths can be absolute or relative.\\n## Absolute Paths\\nAn absolute path is a complete path from the root of the file system to the target file or directory. It starts with a `/` and includes all directories leading to the target.\\nFor example:\\n- `/home/user/documents/file.txt` - This is an absolute path to the file `file.txt` located in the `documents` directory, which is inside the `user` directory, which is inside the `home` directory. \\n\\n## Relative Paths\\nA relative path is a path that is relative to the current working directory. It does not start with a `/` and is used to navigate from the current directory to the target file or directory.\\n\\n"],"11 - Deploying using GitHub pages.webnb":["internal","# Deploying to GitHub Pages\\n\\nSo far, we\'ve been writing and running our code \\"locally\\", meaning that only we can see it. But of course, the whole point of the web is to share information with the world. In order to do that, we\'ll need to learn how to **deploy** our pages to the web.\\n\\nWhat does it mean to deploy a page? So generally speaking, we need two things. First, we need a computer that\'s willing to serve our page to the world. Could that be your laptop? Technically, yes but you probably don\'t want to keep your laptop on 24/7, deal with the amount of data that you might need to send to visitors, or (god forbid) worry about potential hackers. So typically, we instead use a dedicated computer called a **server**. Servers are designed to be on all the time and to handle lots of visitors. They\'re also designed to be secure and to protect your data.\\n\\nSecond, it would be nice if we had an easily findable address for our website so that people could type something (non-shady) into their browser and find our page. This is where **domain names** come in. A domain name is a human-readable name that maps to a server. For example, `google.com` maps to a server that serves Google\'s homepage. We\'ll talk more about domain names later.\\n\\n\\nThere are plenty of services that will give you a server and part of a domain name. For example, here\'s one of many called [a2hosting](https://www.a2hosting.com/). But as you can see, it costs money.\\n\\nFor this specialization, we\'re going to use [Github pages](https://pages.github.com/) because it\'s free, fast, popular, and easy to use particularly if you\'re already using Github. Github Pages is a *separate* service that Github provides to its users.\\n\\nAs a first step to using Github Pages, we\'ll need to push our changes to Github. So let\'s talk about how to do that in the next video\\n\\n## Syncing Code with Github\\n\\nGithub is a website that makes it easier to share your code and collaborate with other programmers. In this class, we\'ll use it not so much for collaboration but for hosting our code. As a first step to using Github, we need to create a \\"respository\\", which is like a folder that will sync between your computer and Github.\\n\\n\\nI\'m going to create a repository named \\"javascript-example\\" on Github\\n\\nNext, I need to connect this repository to my computer. Now, if you\'re comfortable with the terminal, you can follow the directions on Github to do this. But I\'m going to use VS Code instead. So I\'ll open VS Code and click the \\"Source Control\\" icon\\n\\n- click \\"clone repository\\"\\n  - paste in repo URL: **note: this is not the URL of the repository\'s page**\\n- select a destination (where the code will be stored on your computer)\\n  - note that it will create a directory with the same name as the repository\\n- open the directory in VS Code\\n\\n\\n**Note that you only need to do this once per repository.**\\n\\n\\nNext time I want to work on this repository, I can just open the directory in VS Code.\\n\\n\\nSo now we have a directory connected to a Github repository. Next, we need to talk about how to sync our code between our computer and Github. So let\'s write some code....I\'ll write some HTML, some CSS, and some JavaScript.\\n\\nBecause we have one repository for the class, everything that we work on for a particular problem set or exercise should go into a sub-directory inside of that. I\'ll call this one `deploy-example`\\n\\nindex.html:\\n```html\\n<!DOCTYPE html>\\n<html>\\n    <head>\\n        <link rel=\\"stylesheet\\" href=\\"style.css\\">\\n        <script src=\\"script.js\\" defer><\/script>\\n    </head>\\n    <body>\\n        <button id=\\"my-button\\">Click me!</button>\\n        <p id=\\"click-count\\">The button has been clicked 0 times</p>\\n    </body>\\n</html>\\n```\\n\\nstyle.css:\\n```css\\nbody {\\n    background-color: steelblue;\\n    color: gold;\\n    font-size: 3em;\\n}\\n```\\n\\nscript.js:\\n```javascript\\nconst myButton = document.querySelector(\'#my-button\');\\nconst clickCountP = document.querySelector(\'#click-count\');\\nlet count = 0;\\n\\nmyButton.addEventListener(\'click\', function() {\\n    count++;\\n    clickCountP.innerText = \\"The button has been clicked \\" + count + \\" times\\";\\n});\\n```\\n\\nThen, after we save all our code (and we probably also want to verify that it works) then we next need to sync it with Github. Technically, this involves two steps: committing and syncing. Github\'s underlying version tool, called Git, stores a local version of the repository and when we \\"commit\\", we\'re pushing our changes to our local repository. Then, when we sync, we\'re synchronizing our repository with github\'s.\\n\\nWhenever we commit, we\'re required to type a short message describing our changes. Since we\'re not doing a collaborative project and other people don\'t need to read our commit message, we can be pretty lazy about these messages, to be honest. i\'ll just write \\"added files\\"\\n\\nThen, we can commit and sync in one step with this button.\\n\\n\\nOnce I do this, I can check the repository on Github to make sure my latest code is there.\\n\\n\\nNow, if we have change on github that aren\'t on our local machine...for example, if you have multiple computerse that you use to work on code, then we can also use sync to download the latest version.\\n\\nSo for example, here I have a directory that is connected to my github repository but doesn\'t have the latest changes from GitHub. So I can click \\"sync\\" to get the latest changes.\\n\\n\\nSo that\'s a brief overview of how we can use github from within VS Code. So far, we\'ve uploaded our code to Github but we haven\'t yet actually deployed our pages. So let\'s do that next video.\\n\\n## # Deploying to Github Pages\\n- Go to the repository\'s settings page\\n- Scroll down to the \\"Pages\\" section\\n![](gh_pages_section.png)\\n- Select \\"Deploy fom Branch\\" under \\"Source\\"\\n- Select \\"main\\" as the branch and \\"/ (root)\\"; click \\"Save\\"\\n![](gh_pages_config.png)\\n\\n- Look for \\"Your site is live at...\\" in the \\"Pages\\" section\\n![](site_live_at.png)\\n- Click on the link and **add to the URL** the path to the page you want:\\n  - `https://user.github.io/repo/` **+** `rel_dir/code.html`\\n  - `https://user.github.io/repo/rel_dir/code.html`\\n- Try this for the exercise (or really any code)"],"DOM JS CSSOM.png":["external",48741],"gh_pages_config.png":["external",27330],"gh_pages_section.png":["external",7235],"site_live_at.png":["external",17494]}}}')},992:(e,t,n)=>{Object.defineProperty(t,"__esModule",{value:!0}),t.GSFS=t.Directory=t.File=void 0;const o=n(398),a=n(729);class i{constructor(e,t,n){this.uri=e,this.name=t,this.getData=n,this.type=o.FileType.File,this.ctime=Date.now(),this.mtime=Date.now(),this.size=0}}t.File=i;class s{constructor(e,t){this.uri=e,this.type=o.FileType.Directory,this.ctime=Date.now(),this.mtime=Date.now(),this.size=0,this.name=t,this.entries=new Map}}t.Directory=s;const r=new TextEncoder;class l{constructor(e){this.baseURL=e,this.root=new s(o.Uri.parse("gsfs:/"),""),this._emitter=new o.EventEmitter,this._bufferedEvents=[],this.onDidChangeFile=this._emitter.event,this.disposable=o.Disposable.from(o.workspace.registerFileSystemProvider(l.scheme,this,{isCaseSensitive:!0}))}dispose(){this.disposable?.dispose()}seed(){this.createDirectory(o.Uri.parse("gsfs:/samples/"));const e=(t,n="")=>{for(const[a,i]of Object.entries(t))if(Array.isArray(i)){const e="internal"===i[0],t=e?i[1]:void 0,s=r.encode(t);this.writeFile(o.Uri.parse(`gsfs:/${n}/${a}`),s,{create:!0,overwrite:!0,external:!e,size:e?s.byteLength:i[1]})}else this.createDirectory(o.Uri.parse(`gsfs:/${n}/${a}`)),e(i,n+"/"+a)};e(a,"samples")}stat(e){return this._lookup(e,!1)}readDirectory(e){const t=this._lookupAsDirectory(e,!1),n=[];for(const[e,o]of t.entries)n.push([e,o.type]);return n}async readFile(e){const t=await this._lookupAsFile(e,!1).getData();if(t)return t;throw o.FileSystemError.FileNotFound()}writeFile(e,t,n){const a=this._basename(e.path),r=this._lookupParentDirectory(e);let l=r.entries.get(a);if(l instanceof s)throw o.FileSystemError.FileIsADirectory(e);if(!l&&!n.create)throw o.FileSystemError.FileNotFound(e);if(l&&n.create&&!n.overwrite)throw o.FileSystemError.FileExists(e);l||(l=n.external?new i(e,a,(async()=>{const{path:t}=e.toJSON(),n=t.replace(/^\/samples/,""),a=o.Uri.joinPath(o.Uri.parse(this.baseURL),"external",n).toString(),i=await fetch(a);if(!i.ok)throw new Error(`Failed to get content from ${t}`);const s=await i.arrayBuffer();return new Uint8Array(s)})):new i(e,a,(()=>Promise.resolve(t))),l.size=n.size,r.entries.set(a,l),this._fireSoon({type:o.FileChangeType.Created,uri:e})),l.mtime=Date.now(),this._fireSoon({type:o.FileChangeType.Changed,uri:e})}rename(e,t,n){if(!n.overwrite&&this._lookup(t,!0))throw o.FileSystemError.FileExists(t);const a=this._lookup(e,!1),i=this._lookupParentDirectory(e),s=this._lookupParentDirectory(t),r=this._basename(t.path);i.entries.delete(a.name),a.name=r,s.entries.set(r,a),this._fireSoon({type:o.FileChangeType.Deleted,uri:e},{type:o.FileChangeType.Created,uri:t})}delete(e){const t=e.with({path:this._dirname(e.path)}),n=this._basename(e.path),a=this._lookupAsDirectory(t,!1);if(!a.entries.has(n))throw o.FileSystemError.FileNotFound(e);a.entries.delete(n),a.mtime=Date.now(),a.size-=1,this._fireSoon({type:o.FileChangeType.Changed,uri:t},{uri:e,type:o.FileChangeType.Deleted})}createDirectory(e){const t=this._basename(e.path),n=e.with({path:this._dirname(e.path)}),a=this._lookupAsDirectory(n,!1),i=new s(e,t);a.entries.set(i.name,i),a.mtime=Date.now(),a.size+=1,this._fireSoon({type:o.FileChangeType.Changed,uri:n},{type:o.FileChangeType.Created,uri:e})}_lookup(e,t){const n=e.path.split("/");let a=this.root;for(const i of n){if(!i)continue;let n;if(a instanceof s&&(n=a.entries.get(i)),!n){if(t)return;throw o.FileSystemError.FileNotFound(e)}a=n}return a}_lookupAsDirectory(e,t){const n=this._lookup(e,t);if(n instanceof s)return n;throw o.FileSystemError.FileNotADirectory(e)}_lookupAsFile(e,t){const n=this._lookup(e,t);if(n instanceof i)return n;throw o.FileSystemError.FileIsADirectory(e)}_lookupParentDirectory(e){const t=e.with({path:this._dirname(e.path)});return this._lookupAsDirectory(t,!1)}watch(e){return new o.Disposable((()=>{}))}_fireSoon(...e){this._bufferedEvents.push(...e),this._fireSoonHandle&&clearTimeout(this._fireSoonHandle),this._fireSoonHandle=setTimeout((()=>{this._emitter.fire(this._bufferedEvents),this._bufferedEvents.length=0}),5)}_basename(e){return(e=this._rtrim(e,"/"))?e.substr(e.lastIndexOf("/")+1):""}_dirname(e){return(e=this._rtrim(e,"/"))?e.substr(0,e.lastIndexOf("/")):"/"}_rtrim(e,t){if(!e||!t)return e;const n=t.length,o=e.length;if(0===n||0===o)return e;let a=o,i=-1;for(;i=e.lastIndexOf(t,a-1),-1!==i&&i+n===a;){if(0===i)return"";a=i}return e.substring(0,a)}_getFiles(){const e=new Set;return this._doGetFiles(this.root,e),e}_doGetFiles(e,t){e.entries.forEach((e=>{e instanceof i?t.add(e):this._doGetFiles(e,t)}))}}t.GSFS=l,l.scheme="gsfs"}},t={};function n(o){var a=t[o];if(void 0!==a)return a.exports;var i=t[o]={exports:{}};return e[o](i,i.exports,n),i.exports}var o={};(()=>{var e=o;Object.defineProperty(e,"__esModule",{value:!0}),e.activate=async function(e){if("object"==typeof navigator){const n=await t.commands.executeCommand("gamma-samples-extension.location","get","search"),o=await t.commands.executeCommand("gamma-samples-extension.location","get","origin"),i=await t.commands.executeCommand("gamma-samples-extension.location","get","pathname"),s=t.Uri.joinPath(t.Uri.parse(o),i,"extensions","samples-fs-extension");console.log(s.toString());const r=function(e,t){const n=new a.GSFS(t);return e.subscriptions.push(n),n}(e,s.toString());r.seed();const l=new URLSearchParams(n);if(l.has("path")){const e=l.get("path");t.commands.executeCommand("vscode.open",t.Uri.parse(`gsfs:/samples/${e}`))}else t.commands.executeCommand("vscode.open",t.Uri.parse("gsfs:/samples/README.webnb"));void 0===t.workspace.workspaceFolders&&t.commands.executeCommand("vscode.openFolder",t.Uri.parse("gsfs:/samples"),{forceReuseWindow:!0})}};const t=n(398),a=n(992)})();var a=exports;for(var i in o)a[i]=o[i];o.__esModule&&Object.defineProperty(a,"__esModule",{value:!0})})();
//# sourceMappingURL=extension.js.map